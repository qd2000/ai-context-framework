# Context Matrix Fixtures

These fixtures record general context-governance scenarios that must stay independent from any single real project.

Each fixture is intentionally small. Tests materialize a fresh ACF context, apply the scenario described by `metadata.json`, and assert the expected generic behavior.

