---
id: WS006
type: Task
status: ReadyToMerge
attention: Now
owner: codex
title: 默认全局上下文与显式 Workstream 选择
depends_on: []
read_scope:
  - active/Context.md
  - rules/Always_Active.md
  - reference/System_Manual.md
  - ai_context_framework/commands/status_check.py
  - ai_context_framework/runtime.py
  - tests/**
write_scope:
  - assigned: active/workstreams/WS006.md
  - shared: active/Workstreams.md
  - assigned: ai_context_framework/commands/status_check.py
  - shared: ai_context_framework/runtime.py
  - assigned: tests/**
  - shared: README.md
  - shared: CHANGELOG.md
  - shared: docs/**
  - shared: template/**
  - shared: pyproject.toml
  - shared: ai_context_framework.egg-info/**
  - assigned: scripts/worktree_release_smoke.py
  - assigned: ai_context_framework/commands/next_status.py
  - assigned: ai_context_framework/context_budget.py
  - shared: ai_context_framework/version.py
  - shared: uv.lock
merge_targets:
  - master
merge_owner: WS006
coordination: serial
---
# WS006 - 默认全局上下文与显式 Workstream 选择

## 边界说明

本 Workstream 使用强隔离协作协议。AI 执行前应先运行 `acf workstream context WS006` 获取专属上下文入口；完成、ready、done 或切换状态前，应优先运行 `acf workstream guard WS006 --files <本次修改文件...> --json` 对本次文件集做强验收。裸 guard / `--from-git` 只用于快速查看当前 git diff；旧式整工作区排他检查需显式使用 `--workspace --strict-workspace`。

- `read_scope` 是允许读取的默认上下文入口。
- `write_scope` 是允许直接修改的范围。
- Task Workstream 不直接修改 authority 文件；需要主线合并时先写 merge request。
- active workstream 默认禁止重叠写入；共享文件必须显式使用 `shared:` 并指定 merge_owner 或 serial coordination。

---

## Context Packet

### Mission

新 AI 会话默认只暴露全局上下文；只有显式选择、Current_Task 唯一绑定或 verified worktree 才加载专属 Workstream 上下文，并对冲突与无效选择 fail-closed。

### Allowed Read Scope

- active/Context.md
- rules/Always_Active.md
- reference/System_Manual.md
- ai_context_framework/commands/status_check.py
- ai_context_framework/runtime.py
- tests/**

### Allowed Write Scope

- assigned: active/workstreams/WS006.md
- shared: active/Workstreams.md
- assigned: ai_context_framework/commands/status_check.py
- shared: ai_context_framework/runtime.py
- assigned: tests/**
- shared: README.md
- shared: CHANGELOG.md
- shared: docs/**
- shared: template/**
- shared: pyproject.toml
- shared: ai_context_framework.egg-info/**

### Forbidden Scope

- 未在 write_scope 中声明的文件。
- 其他 active workstream 的 owned 文件。
- Task Workstream 的 authority 文件直接写入。

### Evidence Required

- 状态迁移到 ReadyToMerge 前必须有合并请求或明确无主线合并说明。
- 状态迁移到 Done 前必须有 evidence 和 merge_resolution。

### Merge Contract

- ReadyToMerge 表示任务产物完成。
- Done 表示合并或处置完成。

---

## 目标

新 AI 会话默认只暴露全局上下文；只有显式选择、Current_Task 唯一绑定或 verified worktree 才加载专属 Workstream 上下文，并对冲突与无效选择 fail-closed。

---

## 当前发现

无。

---

## 阻塞原因

无。

---

## 取消原因

无。

---

## 合并请求

### 需要合并到哪里

- master

### 候选变更摘要

合入默认 GlobalOnly 路由、显式/Current Task/verified worktree 选择、冲突与无效选择 fail-closed、pointer-only 渐进式披露、文档模板和 v0.0.3.55 发布元数据。

### 需要人工判断的问题

无。

### 已知冲突

无。

### 验证结果

374/374 测试、Python 3.10 53/53、upgrade matrix 28/28、wheel venv lifecycle smoke、strict/doctor/template 均通过。

### 建议合并方式

使用 ACF worktree merge --no-ff；合并后重跑验证、同步 dogfooding authority、升级生产并校准 FCC。

---

## 证据

无。

最终开发验证：Python 3.12 共 374/374；Python 3.10 路由/Worktree 53/53；full upgrade matrix 28/28；template、strict、doctor、minimal smoke 通过；标准 venv wheel 生命周期 GlobalOnly→WorktreeSelected→Selected→no-ff merge→close→audit 通过。制品 SHA-256：wheel 8C1DAC9D8F68986B7B0FB0F0D659BE085ECE5EB6AA855E66D201B29DC314A77C；sdist 851F55F865AC57A9471143C67051AFD44172D304815A143AAA6023486D9AA369。

---

## 完成记录

无。

---

## 输出物

默认全局上下文路由、显式选择与冲突检测、渐进式披露合同、测试和发布证据
---

## Workspace

- slug: global-context-routing
- mode: none
- note: Worktree is optional and is created only by a separate `acf worktree create` call.

---

## 阶段

| ID | 状态 | 阶段 | 依赖 | 输出物 | 证据 | 下一步 |
|---|---|---|---|---|---|---|
| WS006.1 | Done | 默认全局路由与披露合同 | 无。 | GlobalOnly 路由、全局文件指针和回归测试 | GlobalOnly 路由、pointer-only 渐进式披露与旧 CLI 273 项回归通过。 | 实现 status/next 默认全局语义 |
| WS006.2 | Done | 显式选择与冲突检测 | WS006.1 | CLI、Current Task、verified worktree 选择与 fail-closed 结果 | 显式选择、Current Task、verified worktree、冲突/无效选择测试与源码安装态生命周期 smoke 通过。 | 实现选择来源一致性 |
| WS006.3 | Done | 文档、模板与发布验证 | WS006.2 | 渐进式披露教程、版本、构建和安装 smoke | 374/374 Python 3.12 tests; Python 3.10 routing/worktree 53/53; full upgrade matrix 28/28; template/strict/doctor passed; wheel venv lifecycle smoke GlobalOnly→WorktreeSelected→Selected→merge→close→audit passed; wheel SHA256 8C1DAC9D8F68986B7B0FB0F0D659BE085ECE5EB6AA855E66D201B29DC314A77C; sdist SHA256 851F55F865AC57A9471143C67051AFD44172D304815A143AAA6023486D9AA369 | 更新文档并构建发布制品 |
| WS006.4 | Done | 合并与生产发布准备 | WS006.3 | merge plan、生产安装命令、FCC 全局规则同步方案 | Merge plan、v0.0.3.55 wheel/sdist、生产 uv tool 重装命令、合并后 authority 同步和 FCC GlobalOnly 验收步骤已冻结；实际安装与 FCC 验收仅在 master 合并后执行。 | 合并到 master 后执行生产安装与 FCC GlobalOnly 验收 |

---

## Activity Log

- 2026-08-04T19:44:24+08:00: write_scope += assigned: scripts/worktree_release_smoke.py; reason: 扩展安装态 smoke，验证 GlobalOnly、pointer-only 和 verified worktree 渐进式披露。

- 2026-08-05T08:52:06+08:00: write_scope += assigned: ai_context_framework/commands/next_status.py, assigned: ai_context_framework/context_budget.py, shared: ai_context_framework/version.py, shared: uv.lock; merge_owner = WS006; coordination = serial; reason: 补齐全局路由实现、渐进式披露预算与版本发布的实际修改边界。
