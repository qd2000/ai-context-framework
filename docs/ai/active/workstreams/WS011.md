---
id: WS011
type: Task
status: ReadyToMerge
attention: Now
owner: ChatGPT
title: Project Observer 与自治可观测性
depends_on: []
read_scope:
  - AGENTS.md
  - docs/ai/AGENTS.md
  - docs/ai/active/**
  - docs/ai/reference/**
  - ai_context_framework/**
  - tests/**
  - docs/Automation.md
  - README.md
write_scope:
  - assigned: ai_context_framework/observer.py
  - assigned: ai_context_framework/commands/observer.py
  - assigned: ai_context_framework/runtime.py
  - assigned: tests/test_observer_cli.py
  - assigned: scripts/minimal_smoke.py
  - assigned: docs/Automation.md
  - assigned: README.md
  - assigned: docs/ai/reference/System_Manual.md
  - assigned: template/reference/System_Manual.md
  - assigned: docs/ai/reference/ws011_project_observer/**
  - assigned: CHANGELOG.md
  - assigned: ai_context_framework/observer_storage.py
  - assigned: ai_context_framework.egg-info/SOURCES.txt
  - assigned: scripts/release_check.py
  - assigned: ai_context_framework.egg-info/PKG-INFO
  - assigned: ai_context_framework/commands/continuation_workspace.py
  - assigned: tests/test_continuation_cli.py
  - assigned: ai_context_framework/version.py
  - assigned: pyproject.toml
  - assigned: uv.lock
merge_targets:
  - master
coordination: serial
---
# WS011 - Project Observer 与自治可观测性

## 边界说明

本 Workstream 使用强隔离协作协议。AI 执行前应先运行 `acf workstream context WS011` 获取专属上下文入口；完成、ready、done 或切换状态前，应优先运行 `acf workstream guard WS011 --files <本次修改文件...> --json` 对本次文件集做强验收。裸 guard / `--from-git` 只用于快速查看当前 git diff；旧式整工作区排他检查需显式使用 `--workspace --strict-workspace`。

- `read_scope` 是允许读取的默认上下文入口。
- `write_scope` 是允许直接修改的范围。
- Task Workstream 不直接修改 authority 文件；需要主线合并时先写 merge request。
- active workstream 默认禁止重叠写入；共享文件必须显式使用 `shared:` 并指定 merge_owner 或 serial coordination。

---

## Context Packet

### Mission

设计并实现项目级 Global Observer：跨同一项目所有 Worktree 广泛读取、狭窄写入用户级 .acf observer 状态、不参与 Writer 控制；将原始 Git/ACF/Runtime/evidence 转换为详细、可追溯、带语义翻译与置信度的人类进展，并生成自包含静态 HTML；先在 ACF 项目上用开发态工具持续 dogfood，问题清零后再发布并同步全局/PyPI。

### Allowed Read Scope

- AGENTS.md
- docs/ai/AGENTS.md
- docs/ai/active/**
- docs/ai/reference/**
- ai_context_framework/**
- tests/**
- docs/Automation.md
- README.md

### Allowed Write Scope

- assigned: ai_context_framework/observer.py
- assigned: ai_context_framework/commands/observer.py
- assigned: ai_context_framework/runtime.py
- assigned: tests/test_observer_cli.py
- assigned: scripts/minimal_smoke.py
- assigned: docs/Automation.md
- assigned: README.md
- assigned: docs/ai/reference/System_Manual.md
- assigned: template/reference/System_Manual.md
- assigned: docs/ai/reference/ws011_project_observer/**
- assigned: CHANGELOG.md

### Forbidden Scope

- 未在 write_scope 中声明的文件。
- 其他 active workstream 的 owned 文件。
- Task Workstream 的 authority 文件直接写入。

### Evidence Required

- 状态迁移到 ReadyToMerge 前必须有合并请求或明确无主线合并说明。
- 状态迁移到 Done 前必须有 evidence 和 merge_resolution。

### Merge Contract

- ReadyToMerge 表示任务产物完成。
- Done 表示合并或处置完成。

---

## 目标

设计并实现项目级 Global Observer：跨同一项目所有 Worktree 广泛读取、狭窄写入用户级 .acf observer 状态、不参与 Writer 控制；将原始 Git/ACF/Runtime/evidence 转换为详细、可追溯、带语义翻译与置信度的人类进展，并生成自包含静态 HTML；先在 ACF 项目上用开发态工具持续 dogfood，问题清零后再发布并同步全局/PyPI。

---

## 当前发现

无。

---

## 阻塞原因

无。

---

## 取消原因

无。

---

## 合并请求

### 需要合并到哪里

- master

### 候选变更摘要

Project Observer: project-level read-broad/write-narrow/control-none snapshot/history/semantic/dashboard system, ACF self-dogfood, docs and release gates.

### 需要人工判断的问题

无。

### 已知冲突

无。

### 验证结果

529 unit tests pass; full upgrade matrix passes; minimal smoke passes; release_check full gates pass; installed wheel and sdist observer status/snapshot/dashboard package smoke pass; repeated real ACF dogfood and controlled abnormal scenarios pass; no unresolved high/critical Observer issue.

### 建议合并方式

ACF-managed worktree merge into master, then canonical patch release and installed-state dogfood.

---

## 证据

无。

---

## 完成记录

无。

---

## 输出物

通用 Project Observer CLI、稳定 schema/history/semantic 模型、自包含静态 Dashboard、ACF 自 dogfood 证据与最终发布。
---

## Workspace

- slug: project-observer-autonomous-observability
- local binding authority: `acf worktree verify/list` + local registry
- note: machine-specific worktree path/branch/mode are intentionally not persisted as shared Markdown state.

---

## Activity Log

- 2026-08-21T20:52:23+08:00: write_scope += assigned: ai_context_framework/observer_storage.py; reason: Split Observer storage/history/lock primitives into an agent-friendly module after full regression line-size guard exceeded 2000 lines.

- 2026-08-21T21:01:33+08:00: write_scope += assigned: ai_context_framework.egg-info/SOURCES.txt; reason: Package discovery regression requires tracked setuptools source metadata to include the new Observer modules.

- 2026-08-21T23:06:09+08:00: write_scope += assigned: scripts/release_check.py; reason: Release artifact gate must verify the newly added Observer CLI and static dashboard from installed wheel/sdist, not only repository-source smoke.

- 2026-08-21T23:08:53+08:00: write_scope += assigned: ai_context_framework.egg-info/PKG-INFO; reason: Release metadata must match the updated README content before package/publish gates.

- 2026-08-21T23:19:21+08:00: write_scope += assigned: ai_context_framework/commands/continuation_workspace.py, assigned: tests/test_continuation_cli.py; reason: Dogfood release blocker 21533374f7a700c787ff requires a generic continuation prompt exception for reviewed deterministic ACF Workstream lifecycle authority checkpoints at merge boundaries, with regression coverage.

- 2026-08-22T00:02:09+08:00: write_scope += assigned: ai_context_framework/version.py, assigned: pyproject.toml, assigned: uv.lock; reason: WS011 Stage9 canonical patch release bump after all Observer release gates; version changes must use ACF version tooling and remain in the same reviewed release scope.
