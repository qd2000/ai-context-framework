---
id: WS008
type: Task
status: Done
attention: Now
owner: codex
title: Long-running continuation and dirty ownership recovery
depends_on: []
read_scope:
  - reference/Continuation_Control_Design.md
  - docs/Automation.md
  - ai_context_framework/commands/continuation.py
  - ai_context_framework/commands/continuation_coordination.py
  - ai_context_framework/commands/continuation_workspace.py
  - ai_context_framework/continuation_coordination.py
  - ai_context_framework/continuation_recovery.py
  - ai_context_framework/continuation_rounds.py
  - ai_context_framework/runtime.py
  - tests/test_continuation_cli.py
  - template/reference/System_Manual.md
  - README.md
  - CHANGELOG.md
  - ai_context_framework/commands/continuation_recovery.py
write_scope:
  - assigned: active/workstreams/WS008.md
  - shared: active/Workstreams.md
  - assigned: ai_context_framework/commands/continuation.py
  - assigned: ai_context_framework/commands/continuation_coordination.py
  - assigned: ai_context_framework/commands/continuation_workspace.py
  - assigned: ai_context_framework/continuation_coordination.py
  - assigned: ai_context_framework/continuation_recovery.py
  - assigned: ai_context_framework/continuation_rounds.py
  - assigned: ai_context_framework/continuation_workspace.py
  - shared: ai_context_framework/runtime.py
  - assigned: tests/test_continuation_cli.py
  - shared: template/reference/System_Manual.md
  - shared: README.md
  - shared: CHANGELOG.md
  - shared: docs/**
  - shared: pyproject.toml
  - shared: ai_context_framework/version.py
  - shared: uv.lock
  - shared: ai_context_framework.egg-info/**
  - assigned: ai_context_framework/commands/continuation_recovery.py
merge_targets:
  - master
merge_owner: WS008
coordination: serial
merge_resolution: merged
---
# WS008 - Long-running continuation and dirty ownership recovery

## 边界说明

本 Workstream 使用强隔离协作协议。AI 执行前应先运行 `acf workstream context WS008` 获取专属上下文入口；完成、ready、done 或切换状态前，应优先运行 `acf workstream guard WS008 --files <本次修改文件...> --json` 对本次文件集做强验收。裸 guard / `--from-git` 只用于快速查看当前 git diff；旧式整工作区排他检查需显式使用 `--workspace --strict-workspace`。

- `read_scope` 是允许读取的默认上下文入口。
- `write_scope` 是允许直接修改的范围。
- Task Workstream 不直接修改 authority 文件；需要主线合并时先写 merge request。
- active workstream 默认禁止重叠写入；共享文件必须显式使用 `shared:` 并指定 merge_owner 或 serial coordination。

---

## Context Packet

### Mission

Allow long 1-2+ hour continuation rounds with independent heartbeat/renew timing and evidence-based recovery that preserves and coexists with non-overlapping pre-existing, manual, unrelated, or orphan-owned dirty changes; only true path/effect/identity conflicts may block.

### Allowed Read Scope

- reference/Continuation_Control_Design.md
- docs/Automation.md
- ai_context_framework/commands/continuation.py
- ai_context_framework/commands/continuation_coordination.py
- ai_context_framework/commands/continuation_workspace.py
- ai_context_framework/continuation_coordination.py
- ai_context_framework/continuation_recovery.py
- ai_context_framework/continuation_rounds.py
- ai_context_framework/runtime.py
- tests/test_continuation_cli.py
- template/reference/System_Manual.md
- README.md
- CHANGELOG.md

### Allowed Write Scope

- assigned: active/workstreams/WS008.md
- shared: active/Workstreams.md
- assigned: ai_context_framework/commands/continuation.py
- assigned: ai_context_framework/commands/continuation_coordination.py
- assigned: ai_context_framework/commands/continuation_workspace.py
- assigned: ai_context_framework/continuation_coordination.py
- assigned: ai_context_framework/continuation_recovery.py
- assigned: ai_context_framework/continuation_rounds.py
- assigned: ai_context_framework/continuation_workspace.py
- shared: ai_context_framework/runtime.py
- assigned: tests/test_continuation_cli.py
- shared: template/reference/System_Manual.md
- shared: README.md
- shared: CHANGELOG.md
- shared: docs/**
- shared: pyproject.toml
- shared: ai_context_framework/version.py
- shared: uv.lock
- shared: ai_context_framework.egg-info/**

### Forbidden Scope

- 未在 write_scope 中声明的文件。
- 其他 active workstream 的 owned 文件。
- Task Workstream 的 authority 文件直接写入。

### Evidence Required

- 状态迁移到 ReadyToMerge 前必须有合并请求或明确无主线合并说明。
- 状态迁移到 Done 前必须有 evidence 和 merge_resolution。

### Merge Contract

- ReadyToMerge 表示任务产物完成。
- Done 表示合并或处置完成。

---

## 目标

Allow long 1-2+ hour continuation rounds with independent heartbeat/renew timing and evidence-based recovery that preserves and coexists with non-overlapping pre-existing, manual, unrelated, or orphan-owned dirty changes; only true path/effect/identity conflicts may block.

---

## 当前发现

- ACF v0.0.3.62 已解决 TTL-active 不等价于 runner liveness、stale/orphan classification、generation fencing、durable effect journal 和 formal reconcile/recover；WS008.3 分支已进一步取消 reconcile 对 `git_clean=false` 的 blanket fail-closed，改为消费 bounded workspace ownership manifest/digest。
- stale/orphan runner 的已声明 write intent 与可证明 `runner_owned` WIP 现在可 formal recover 到 generation+1；`baseline_external` / `unexpected_nonoverlap` 原样保留且不进入提交。reconcile receipt 纳入 workspace ownership digest，内容/状态/ownership 漂移会使 receipt stale；无 manifest、generation mismatch、真实路径冲突、不可解释 HEAD、未解决 side effect 或 identity 漂移仍 fail-closed。
- WS008.4A 分支已把 heartbeat/stale/renew timing 解耦，并加入 `standard/long-running` profile、existing-task `configure` 与 generated prompt timing；后续不再把 scheduler interval 当 Gate 截止时间。
- 新的硬约束：`worktree dirty` 不参与 owner liveness、claim/release/reconcile/recover 判断；只使用 path/status/digest ownership、HEAD/effect/identity 等具体证据。正常 Scheduled round 允许携带未提交 `task_owned` WIP release/claim，Git commit 只在自然语义 checkpoint 时创建。
- 同一 worktree 的后续 contention 采用 single-writer + multi-contender：后来的 attempt 先登记 started_at/objective 并 challenge 当前 owner；owner-authenticated ACF activity 可 ACK，timeout 只撤销 ownership 资格而不宣称证明 Agent 死亡，随后仍走 workspace/effect/identity formal reconcile + generation fencing。
- WS008.4C 已落地 bounded 用户级 `coordination.json`：`coordination attempt` 只登记 `claim_candidate` / `contender`，`coordination challenge` 只针对 attempt 已观测到的 fenced owner generation 开启或加入单一 nonterminal challenge；两者都不授予写权限、不宣称 owner 死亡，owner generation 变化会使旧 attempt fail-closed。
- WS008.4D 已把 owner-authenticated continuation activity 接入 challenge response：合法 fenced owner activity 可将 open/timed-out challenge 解析为 `owner_active`，正常 release 解析为 `owner_released`；deadline 到期只产生 `ownership_forfeiture_candidate`，不等价于证明 Agent 死亡。legacy unfenced lease 继续走兼容 renew/release 路径，但不能冒充 generation-bound challenge ACK。
- WS008.4E 已把 challenge timeout 证据接入 formal reconcile/recover：reconcile observation 纳入稳定 coordination digest 与 bounded forfeiture candidates；匹配当前 generation 的 timed-out challenge 可撤销 owner 的 ownership 资格而无需额外宣称 `owner_ended`，但 workspace/HEAD/effect/identity 校验仍全部保留。若旧 owner 在 recover 前 authenticated ACK，coordination digest 漂移会使 receipt `reconciliation_stale`；若 recover 先完成，则 generation+1 fencing 立即拒绝旧 owner，challenge 记录为 `ownership_recovered`。ACK/reconcile/recover 都使用同一 continuation state lock，因此竞态最多产生一个 writer。
- WS008.4F 已把 contention 协议收敛为通用 generated protocol：所有可定位项目的普通 ACF 命令都会 opportunistic 只读提示当前 active owner generation 的 pending challenge，但绝不 ACK；owner-protected continuation 命令在 fenced credential 验证后 authenticated touch，release 解析为 `owner_released`。`acf continuation prompt` 现在完整生成 attempt/challenge、probe、ACK/timeout、formal reconcile/recover、workspace/effect、heartbeat/renew、checkpoint/release 协议，并成为唯一 generic Scheduled Task protocol 权威；项目 wrapper 只保留固定身份与项目特有 Runtime/科学/权限/验收约束。
- WS008.5 adoption preflight 已在四个真实任务上冻结到 `reference/ws008_long_running_dirty_recovery/ADOPTION_MATRIX.md`：WS008 开发版可原位读取 FCC WS079/WS080/WS086 的既有 continuation state 并生成完整 4F prompt，且保留各自 task-specific next_action；其中 WS080/WS086 真实 task-id 分别是 `ws080-fcc-mechanistic-hourly` / `WS086-hourly-continuation`，证明 wrapper 不得假定 task-id 等于 Workstream ID。AStockT_AI qdspc 当前仍安装 v0.0.3.61，WS003 clean/can_claim=true 但生成的是旧协议，同时项目 `Autonomous_Execution.md` 仍复制旧 generic state machine，因此实际 cutover 必须等新稳定版 package smoke + 安装后再执行，不能提前让生产 wrapper 依赖尚未安装的 4F prompt。
- WS008.5 release/adoption cutover 已完成：v0.0.3.63 full release gate 全绿，tag `v0.0.3.63` 已通过仓库 Trusted Publishing 发布到 PyPI，wheel/sdist 均可见；lenovo 全局 ACF 已从 v0.0.3.62 升级到 v0.0.3.63，qdspc 已从 v0.0.3.61 升级到 v0.0.3.63。安装态 ACF 已对 WS079 / `ws080-fcc-mechanistic-hourly` / `WS086-hourly-continuation` / AStock WS003 四个真实既有 state 完成 doctor/prompt re-smoke，无 force re-init 且保留原 scientific/runtime blocker/next_action。FCC 项目 wrapper authority 已提交 `ae0d6806`，AStockT_AI `Autonomous_Execution.md` 已提交 `53a7b80`；四个已保存 Scheduled Task wrapper 也已核验为 doctor+prompt/generated-authority 模式，无需重复改写产品任务文本。

---

## 阻塞原因

无。

---

## 取消原因

无。

---

## 合并请求

### 需要合并到哪里

- master

### 候选变更摘要

WS008 completes long-running continuation dirty-ownership recovery, 4F generated protocol authority, deterministic failure injection, v0.0.3.63 release/publication, and four-project adoption.

### 需要人工判断的问题

无。

### 已知冲突

无。

### 验证结果

v0.0.3.63 full release_check passed; PyPI wheel/sdist published; lenovo and qdspc installed v0.0.3.63; WS079/WS080/WS086/AStock installed-state doctor/prompt re-smoke and wrapper authority cutover complete.

### 建议合并方式

ACF-managed worktree merge with normal post-checks

---

## 证据

pypi:ai-context-framework==0.0.3.63

---

## 完成记录

WS008 merged to master via ACF-managed integration/promotion at 157daf9; v0.0.3.63 published, dual-host installed, and four-project generated-protocol adoption verified.

---

## 输出物

Long-running continuation profile, dirty baseline/write-intent ownership model, safe orphan WIP transfer, unified generated prompt/docs, failure-injection tests, dogfood and release
---

## Workspace

- slug: long-running-dirty-recovery
- mode: worktree
- branch: `codex/ws008-long-running-dirty-recovery`
- path: `D:\PROJECT\Tools\ai-context-framework_worktrees\ws008-long-running-dirty-recovery`
- base: `master@635d798aa0f5a183bfdcd314e7fdc7689580bc68`

---

## 阶段

| ID | 状态 | 阶段 | 依赖 | 输出物 | 证据 | 下一步 |
|---|---|---|---|---|---|---|
| WS008.1 | Done | Dirty taxonomy and long-run protocol | 无。 | Frozen policy for baseline external dirty, runner-owned WIP, unexpected non-overlap dirty, true conflicts, heartbeat/renew timing and stale fencing | Four deterministic expected-failure tests now characterize pre-existing external dirty, unrelated post-claim dirty, stale-owner dirty WIP recovery, and independent stale/renew timing; focused suite OK with 4 expected failures. | Implement WS008.2 bounded workspace baseline/write-intent ownership metadata so non-overlapping dirty is preserved without blanket blocking; keep overlap/ambiguous provenance fail-closed. |
| WS008.2 | Done | Workspace baseline and write-intent ownership | WS008.1 | Bounded baseline dirty manifest, concrete write-intent paths, Workstream-scope filtering and ownership classification that preserve unrelated/manual dirty changes without blanket worktree blocking | Continuation suite 30 OK with 2 later-stage expected failures; package+continuation 38 OK; full unittest 443 OK; template/strict and workspace CLI help pass. | Implement WS008.3 formal interrupted-WIP ownership transfer so stale task-owned WIP can be inherited without touching disjoint external dirty. |
| WS008.3 | Done | Interrupted WIP recovery | WS008.2 | Safe recovery of classified task-owned WIP while unrelated dirty files remain preserved | Reconcile now records workspace ownership digest and permits valid dirty orphan WIP; recover transfers prior write intents/runner-owned classifications to generation+1, preserves disjoint external dirty, fences the old owner, and rejects stale receipts after workspace digest drift. Continuation 31 OK with only WS008.4 expected failure; full unittest 444 OK with expected failures=1; template/strict pass. | Implement WS008.4 independent heartbeat/stale/renew timing, in-place configure, long-running profile, and generated protocol authority. |
| WS008.4 | Done | Long-running timing, commitless handoff and contention coordination | WS008.3 | Independent timing/configure; task-owned uncommitted WIP handoff without worktree dirty gates; attempt/challenge single-writer contention protocol; ACF-generated recovery-first prompt | WS008.4A-4F complete. 4D authenticates owner challenge responses and timeout forfeiture candidates; 4E binds those candidates into formal reconcile/recover with coordination-digest stale receipts and generation fencing; 4F adds universal read-only challenge probe and makes generated prompt the sole generic Scheduled Task protocol authority. Continuation CLI regression 48 OK; final full unittest 461 OK; runtime remains below the 2000-line module gate. | Enter WS008.5 failure injection, project adoption and release. |
| WS008.5 | Done | Failure injection, project adoption and release | WS008.4 | Overlap matrix, long-run/crash recovery tests, WS079/WS080/WS086/AStock prompt adoption, docs/help synchronization, package release and merge | Failure injection, v0.0.3.63 full release/PyPI publication, dual-host install, four-project installed-state adoption and wrapper-authority cutover complete. | Finalize WS008 evidence/state, request and perform ACF-managed merge to master, archive/close the Workstream worktree, and verify post-merge cleanup. |

---

## Activity Log

- 2026-08-18T13:26:08+08:00: read_scope += ai_context_framework/commands/continuation_recovery.py; write_scope += assigned: ai_context_framework/commands/continuation_recovery.py; reason: WS008.4E keeps the continuation controller below the agent-friendly module-size gate by isolating formal recovery command orchestration.
