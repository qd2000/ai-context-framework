---
id: WS005
type: Task
status: Active
owner: codex
title: ACF Workstream与Git Worktree生命周期管理
depends_on: []
read_scope:
  - README.md
  - ai_context_framework/**
  - tests/**
  - docs/ai/**
  - template/**
write_scope:
  - owned: docs/ai/active/workstreams/WS005.md
  - shared: docs/ai/active/Workstreams.md
  - owned: ai_context_framework/**
  - owned: tests/**
  - shared: README.md
  - shared: docs/ai/**
  - shared: template/**
  - shared: pyproject.toml
  - shared: uv.lock
  - shared: MANIFEST.in
  - shared: docs/Worktree_Lifecycle.md
  - assigned: scripts/worktree_release_smoke.py
  - assigned: ai_context_framework.egg-info/**
  - shared: docs/Automation.md
merge_targets:
  - docs/ai/AGENTS.md
  - docs/ai/rules/Always_Active.md
  - README.md
  - docs/Worktree_Lifecycle.md
  - template/AGENTS.md
  - template/rules/Always_Active.md
merge_owner: WS005
coordination: serial
---
# WS005 - ACF Workstream与Git Worktree生命周期管理

## 边界说明

本 Workstream 使用强隔离协作协议。AI 执行前应先运行 `acf workstream context WS005` 获取专属上下文入口；完成、ready、done 或切换状态前，应优先运行 `acf workstream guard WS005 --files <本次修改文件...> --json` 对本次文件集做强验收。裸 guard / `--from-git` 只用于快速查看当前 git diff；旧式整工作区排他检查需显式使用 `--workspace --strict-workspace`。

- `read_scope` 是允许读取的默认上下文入口。
- `write_scope` 是允许直接修改的范围。
- Task Workstream 不直接修改 authority 文件；需要主线合并时先写 merge request。
- active workstream 默认禁止重叠写入；共享文件必须显式使用 `shared:` 并指定 merge_owner 或 serial coordination。

---

## Context Packet

### Mission

在不改变现有Workstream接口和无worktree逻辑的前提下，实现可选的Workstream编号预约以及供AI调用的Git worktree创建、绑定、验证、同步、合并、关闭、审计、幂等恢复和生产升级能力。

### Allowed Read Scope

- README.md
- ai_context_framework/**
- tests/**
- docs/ai/**
- template/**

### Allowed Write Scope

- owned: docs/ai/active/workstreams/WS005.md
- shared: docs/ai/active/Workstreams.md
- owned: ai_context_framework/**
- owned: tests/**
- shared: README.md
- shared: docs/ai/**
- shared: template/**
- shared: pyproject.toml
- shared: uv.lock

### Forbidden Scope

- 未在 write_scope 中声明的文件。
- 其他 active workstream 的 owned 文件。
- Task Workstream 的 authority 文件直接写入。

### Evidence Required

- 状态迁移到 ReadyToMerge 前必须有合并请求或明确无主线合并说明。
- 状态迁移到 Done 前必须有 evidence 和 merge_resolution。

### Merge Contract

- ReadyToMerge 表示任务产物完成。
- Done 表示合并或处置完成。

---

## 目标

在不改变现有Workstream接口和无worktree逻辑的前提下，实现可选的Workstream编号预约以及供AI调用的Git worktree创建、绑定、验证、同步、合并、关闭、审计、幂等恢复和生产升级能力。

---

## 当前发现

- `acf workstream add` 与现有 Workstream 生命周期保持原行为；Git 配置缺失不会影响不使用 worktree 的逻辑。
- 新增 `workstream reserve` 与独立 `worktree` 命令组，通过 Git common-dir journal、registry 和锁实现可恢复事务。
- 正式/非 WS worktree、错误仓库、detached、路径占用、分支占用、primary 前进、冲突预演、检查失败和部分关闭均已有真实临时 Git 仓库回归。
- 生产升级必须在分支合入 `master` 后执行；当前分支只完成发布制品和隔离安装验证。

---

## 阻塞原因

无。

---

## 取消原因

无。

---

## 合并请求

### 需要合并到哪里

- README.md
- docs/Worktree_Lifecycle.md
- template/AGENTS.md
- template/rules/Always_Active.md

### 候选变更摘要

合并ACF可选Workstream/Worktree生命周期功能、模板规则和发布文档。

### 需要人工判断的问题

无。

### 已知冲突

无。

### 验证结果

353项回归、Python 3.10矩阵、完整升级矩阵、源码与wheel安装态生命周期smoke全部通过。

### 建议合并方式

将codex/ws005-worktree-lifecycle通过no-ff合入master。

---

## 证据

- 完整单元测试：353/353，通过（既有 CLI 273、Worktree 生命周期 34、其余 fixture/升级/打包 46）。
- Python 3.10：Worktree、entrypoint 和 package 核心矩阵 46/46 通过。
- `scripts/minimal_smoke.py` 与完整 `scripts/upgrade_matrix.py --mode full` 通过。
- template 普通检查与 dogfooding `docs/ai --strict` 检查通过；compileall、package skeleton 和 `git diff --check` 通过。
- 源码 console script 与隔离 wheel 安装均完成 `reserve -> create -> verify -> ReadyToMerge -> no-ff merge -> close -> audit`。
- wheel：`ai_context_framework-0.0.3.54-py3-none-any.whl`，SHA-256 `2D840CD56DD14B8DF4F56055D49F741629A0BCBD5C9D66E56F5086CE062DD899`。
- sdist：`ai_context_framework-0.0.3.54.tar.gz`，SHA-256 `2AFAB0207B416ECBC8FEC4AE894D4F8EF4C122543161D45A4DFCD9AC415C6180`。

---

## 完成记录

无。

---

## 输出物

兼容的CLI、Git配置、状态机、operation journal、完整测试、文档和生产安装

---

## 阶段

| ID | 状态 | 阶段 | 依赖 | 输出物 | 证据 | 下一步 |
|---|---|---|---|---|---|---|
| WS005.1 | Done | 兼容性基线与Git基础设施 | 无 | 独立Git adapter、配置、journal、registry、lock与只读发现 | Git adapter、配置、journal、registry、锁和只读发现已实现；既有入口兼容测试通过。 | 进入WS005.2 |
| WS005.2 | Done | 编号预约与Worktree创建恢复 | WS005.1 | reserve/create/attach/verify/list/audit及幂等恢复 | 编号预约、故障恢复、正式/非WS worktree创建、绑定、验证和审计已由临时Git仓库测试验证。 | 进入WS005.3 |
| WS005.3 | Done | 同步合并与安全关闭 | WS005.2 | sync/merge-plan/merge/close与冻结commit门禁 | 同步冲突预演、冻结commit、no-ff合并、pre/post check和安全关闭已由临时Git仓库测试验证。 | 进入WS005.4 |
| WS005.4 | Done | 文档、发布验证与合并准备 | WS005.3 | v0.0.3.54、完整回归、升级矩阵、构建、Python 3.10 与 wheel 隔离安装 | v0.0.3.54完整回归353/353、Python 3.10核心矩阵46/46、full upgrade matrix、源码/wheel完整生命周期smoke、template/docs严格检查及最终制品哈希均通过。 | 提交分支、合并到master并升级生产uv tool。 |

---

## Activity Log

- 2026-08-04T16:07:37+08:00: write_scope += shared: MANIFEST.in, shared: docs/Worktree_Lifecycle.md, assigned: scripts/worktree_release_smoke.py, assigned: ai_context_framework.egg-info/**; merge_owner = WS005; coordination = serial; reason: 发布闭环需要源码包清单、生命周期设计、安装态 smoke 和本地打包元数据。

- 2026-08-04T16:08:02+08:00: write_scope += shared: docs/Automation.md; merge_owner = WS005; coordination = serial; reason: 发布文档需要同步自动化与测试入口说明。
