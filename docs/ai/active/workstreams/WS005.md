---
id: WS005
type: Task
status: Open
owner: codex
title: ACF Workstream与Git Worktree生命周期管理
depends_on: []
read_scope:
  - README.md
  - ai_context_framework/**
  - tests/**
  - docs/ai/**
  - template/**
write_scope:
  - owned: docs/ai/active/workstreams/WS005.md
  - shared: docs/ai/active/Workstreams.md
  - owned: ai_context_framework/**
  - owned: tests/**
  - shared: README.md
  - shared: docs/ai/**
  - shared: template/**
  - shared: pyproject.toml
  - shared: uv.lock
merge_owner: WS005
coordination: serial
---
# WS005 - ACF Workstream与Git Worktree生命周期管理

## 边界说明

本 Workstream 使用强隔离协作协议。AI 执行前应先运行 `acf workstream context WS005` 获取专属上下文入口；完成、ready、done 或切换状态前，应优先运行 `acf workstream guard WS005 --files <本次修改文件...> --json` 对本次文件集做强验收。裸 guard / `--from-git` 只用于快速查看当前 git diff；旧式整工作区排他检查需显式使用 `--workspace --strict-workspace`。

- `read_scope` 是允许读取的默认上下文入口。
- `write_scope` 是允许直接修改的范围。
- Task Workstream 不直接修改 authority 文件；需要主线合并时先写 merge request。
- active workstream 默认禁止重叠写入；共享文件必须显式使用 `shared:` 并指定 merge_owner 或 serial coordination。

---

## Context Packet

### Mission

在不改变现有Workstream接口和无worktree逻辑的前提下，实现可选的Workstream编号预约以及供AI调用的Git worktree创建、绑定、验证、同步、合并、关闭、审计、幂等恢复和生产升级能力。

### Allowed Read Scope

- README.md
- ai_context_framework/**
- tests/**
- docs/ai/**
- template/**

### Allowed Write Scope

- owned: docs/ai/active/workstreams/WS005.md
- shared: docs/ai/active/Workstreams.md
- owned: ai_context_framework/**
- owned: tests/**
- shared: README.md
- shared: docs/ai/**
- shared: template/**
- shared: pyproject.toml
- shared: uv.lock

### Forbidden Scope

- 未在 write_scope 中声明的文件。
- 其他 active workstream 的 owned 文件。
- Task Workstream 的 authority 文件直接写入。

### Evidence Required

- 状态迁移到 ReadyToMerge 前必须有合并请求或明确无主线合并说明。
- 状态迁移到 Done 前必须有 evidence 和 merge_resolution。

### Merge Contract

- ReadyToMerge 表示任务产物完成。
- Done 表示合并或处置完成。

---

## 目标

在不改变现有Workstream接口和无worktree逻辑的前提下，实现可选的Workstream编号预约以及供AI调用的Git worktree创建、绑定、验证、同步、合并、关闭、审计、幂等恢复和生产升级能力。

---

## 当前发现

无。

---

## 阻塞原因

无。

---

## 取消原因

无。

---

## 合并请求

无。

---

## 证据

无。

---

## 完成记录

无。

---

## 输出物

兼容的CLI、Git配置、状态机、operation journal、完整测试、文档和生产安装
