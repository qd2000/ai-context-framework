本文件记录当天整理后的项目工作记录。

请注意：本文件是历史过程记录，不是当前事实源。  
当前事实请查看 `active/Context.md`。

---

## 今日完成

- v0.0.3.95 已成功发布到 PyPI（latest=0.0.3.95），发布链闭合：定位到 v0.0.3.93/.94 发布失败的共同根因是 WS014 新引入的 release.yml verify 门禁在 Windows runner 上暴露的 3 处既有测试夹具缺陷（8.3 短路径别名 + 终止进程句柄瞬时占用），修复并重发。

- 定位并修复 Windows CI 发布门禁：`tests/test_worktree_resilient_merge.py` 改用规范临时根 + 文件身份判断注入点；`tests/test_install_scripts.py` 改用 `samefile` 校验 `acf.cmd` 调用的工具 python；`tests/test_continuation_execution.py` 的 tearDown 增加有界重试。
- 版本收敛到 `v0.0.3.95`（`93f04fd` → `17024d2`），tag 推送后 release workflow 通过验证并发布成功：PyPI `latest=0.0.3.95`。
- 用 `acf log issue resolve` 以 `git:17024d2` 与 `pypi:ai-context-framework:0.0.3.95` 等证据收口 `d33a11a387320d21a862`（worktree curated handoff 退休路径），跨项目 open issue 由 4 条降到 3 条。

- WS016 第 1 步完成：runtime 与 runtime_parts 的 __dict__ 双向合并收窄为显式导出 allowlist（新增 runtime_exports.py，202 个必需导出名），四种违约情形 fail-closed；新增 runtime_export_audit.py 与 9 项契约回归。

---

## 今日讨论 / AI 协作

- 通过 `acf.py new worklog` 生成本工作记录，并更新 `worklog/Worklog_Index.md`。

---

## 有价值的结论

- 发布门禁失败的根因是 Windows hosted runner 的 8.3 短路径别名（RUNNER~1 vs runneradmin）让 overlap 竞态注入静默失效（在 runner 上该用例空跑并误判合并被允许）、install 脚本用例做了裸路径字符串比对、以及 tearDown 的临时目录被刚终止进程瞬时占用；三处均属测试夹具缺陷，产品行为不变，已用本机 GetShortPathNameW 构造别名条件复现验证。

如果这些结论已经成为当前事实，请同步更新到 `active/Context.md`。

- runtime 公开命名空间从 769 降到 531、runtime.py 从 1988 降到 1979 行，且全量门禁（689 项 unittest + check template/strict + smoke 8/8 + upgrade matrix 28 fixtures）保持全绿，证明这是零行为变更的内部重构；期间发现并显式保留了 archive_workstream.__getattr__ 这条 PEP 562 惰性转发依赖。

---

## 重要决策候选

- 无。

如果确认生效，请同步更新到：

- `reference/Decisions_Index.md`
- `decisions/`

---

## 无效尝试

- 无。

---

## 新发现的问题

- 无。

---

## 原始日志位置

- 无。

注意：不要把原始日志全文写入本文件。

---

## 需要同步更新的文件

- `active/Context.md`：视情况更新。
- `reference/Decisions_Index.md`：视情况更新。
- `rules/`：视情况更新。
- `archive/`：视情况归档。

---

## 下一步候选

- 无。

注意：下一步候选不等于当前任务。  
如果某个候选下一步被选为当前任务，请写入 `active/Current_Task.md`。
