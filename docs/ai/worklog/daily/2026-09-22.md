本文件记录当天整理后的项目工作记录。

请注意：本文件是历史过程记录，不是当前事实源。  
当前事实请查看 `active/Context.md`。

---

## 今日完成

- WS018 阶段 B：回填 v0.0.3.97 发布证据，落地 PEP 639 许可元数据并发布 v0.0.3.98

- 回填 v0.0.3.97 的真实发布与 installed-state 证据：tag 仍指向 `33163fc8`（未移动、未删除、未重建），CI #69 attempt 2 / success，`Publish release to PyPI` #39 attempt 2 / success，PyPI `0.0.3.97` 的 wheel/sdist 摘要与 `digests.sha256` 逐字符一致。

- 用 canonical `scripts/update_acf.ps1` 更新全局安装并验证 `acf --version`、`acf clock now`、`acf clock now --json`（先 `.97`，发布后 `.98`）。

- 落地 PEP 639 许可元数据：`license = "MIT"`、`license-files = ["LICENSE"]`、`setuptools>=77.0.3`，`MANIFEST.in` 显式 `include LICENSE`；新增 `tests/test_release_license_metadata.py` 四项确定性回归（pyproject 字段与下限、egg-info 许可行、wheel METADATA、wheel 与 sdist 的 LICENSE 收录）。

- 版本收敛 `v0.0.3.98`（含 `uv.lock`）；本地 `release_check --mode full` 与公开 CI #70 全绿；创建 immutable tag `v0.0.3.98` 并经 Trusted Publishing 发布（release #40，PyPI `latest=0.0.3.98`）；全局安装升级到 `acf v0.0.3.98`。

---

## 今日讨论 / AI 协作

- 通过 `acf.py new worklog` 生成本工作记录，并更新 `worklog/Worklog_Index.md`。

---

## 有价值的结论

- v0.0.3.98 完成 tag / PyPI / 全局安装三项验证，wheel 与 sdist 首次确定地携带 MIT 许可

如果这些结论已经成为当前事实，请同步更新到 `active/Context.md`。

---

## 重要决策候选

- 无。

如果确认生效，请同步更新到：

- `reference/Decisions_Index.md`
- `decisions/`

---

## 无效尝试

- 无。

---

## 新发现的问题

- 无。

- `v0.0.3.97` 的 wheel/sdist 均不含 LICENSE、METADATA 也没有任何 License 字段：该 tag 早于 LICENSE 落地提交 `155480b`。这是 `.98` 必须发布的真实原因，不是绕过问题。

- 本地 `release_check --mode full` 的构建步骤首次失败于外部删除守卫（`[safe-delete][SAFE_DELETE_BULK_CONFIRM_REQUIRED] {"count":500,"threshold":500}`）拦截 setuptools 清理临时源树；重跑同一构建成功、摘要一致，判定为环境干扰而非打包缺陷。

- `docs/ai/active/Context.md` 是 authority 路径，`acf workstream scope-add` 必须用 `authority:` 类型声明，用 `owned:` 会被 `acf check --strict` 拦下。

---

## 原始日志位置

- 无。

注意：不要把原始日志全文写入本文件。

---

## 需要同步更新的文件

- `active/Context.md`：视情况更新。
- `reference/Decisions_Index.md`：视情况更新。
- `rules/`：视情况更新。
- `archive/`：视情况归档。

---

## 下一步候选

- 无。

注意：下一步候选不等于当前任务。  
如果某个候选下一步被选为当前任务，请写入 `active/Current_Task.md`。

- WS018 closeout：ready / done / archive 与 worktree、本地 branch 清理。

- README 历史 CRLF 行尾问题仍是独立后续项，未在本期处理。
