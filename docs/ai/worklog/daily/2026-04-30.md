本文件记录当天整理后的项目工作记录。

请注意：本文件是历史过程记录，不是当前事实源。  
当前事实请查看 `active/Context.md`。

---

## 今日完成

- 沉淀多 agent 并行 Workstream 设计计划
- 完成 T002 模板与读取规则收敛：Workstream 默认不启用，仅在 Active/Blocked/ReadyToMerge 或并行协作整理时按需读取。
- 完成 T007 最小 Workstream dogfooding gate：创建 WS001，记录 Open -> Active -> ReadyToMerge -> Done、合并请求和 evidence。
- 完成 T005 front matter 基础设施：新增无依赖 parse/format/validate 函数、schema dataclass 和诊断码，并用单元测试覆盖合法/非法语法、稳定格式化、required/enum/list、typed write_scope 和路径诊断。
- 完成 T006 upgrade 迁移与兼容性矩阵：明确默认 upgrade 不启用 Workstream、显式 init 才创建 Workstream 结构、front matter 检查触发条件、JSON 字段语义和测试矩阵。
- 完成 T003 workstream CLI/check 实现规格：补齐命令范围、通用写命令契约、每个命令的参数/改动文件/错误码、状态转换表、scope claim 规则、check severity 和测试计划，未写 CLI 实现。
- 完成 T008 ADR 候选：生成 Proposed 状态的 ADR-0005，记录可选 Workstream 层的稳定取舍，不复制详细设计。
- 完成 T004 第一刀：实现 Workstream 数据模型、schema、scope normalize、initialized 判断，以及 `acf workstream init/status/list/show`；新增测试覆盖未 init 可选行为、init 幂等、list/show front matter 解析和 schema 错误。
- 完成 T004 第二刀：实现 `acf workstream add/set/block/cancel`，新增详情/索引同步、duplicate ID 拒绝、dry-run、基础状态转换、reason 必填落盘和 Done/Cancelled 终态保护测试。
- 完成 T004 第三刀：实现 `acf workstream merge-request/ready/done`，新增合并请求覆盖写入、ReadyToMerge 前置校验、evidence 必填落盘、Done 终态保护和 show/list 状态证据测试。
- 完成 T004 第四刀：实现 `acf workstream claim/note`，新增白名单 note、自动创建缺失 section、typed scope、owned 校验、draft warning、authority/assigned 冲突和 dry-run 测试。
- 完成 T004 第五刀：`acf check` 接入 optional Workstream severity 矩阵，覆盖未启用不检查、索引/详情一致性、状态必填内容、scope 冲突和 draft warning/strict error。
- 完成 Workstream 第一版收尾：T004 标记 Done，ADR-0005 从 Proposed 升为 Active。

- 开启后续计划：真实项目接入与旧上下文升级验证；旧 Workstream 计划已归档到 archive/plans，并创建新的 Active Task_Plan。
- 新增 reference/Real_Project_Upgrade_Playbook.md，沉淀 ACF v0.0.3.12 真实项目旁路接入、旧上下文升级、Workstream/front matter optional 边界和 Upgrade Acceptance 验收表。
- 完成两项真实项目升级试点：knowledgeConnector 走旧 ACF upgrade 路径并通过 strict check；register 已是可通过 v0.0.3.12 检查的旧 ACF 上下文，upgrade dry-run 无需变更。
- 完成新项目 init 试点：papers 从无 ACF 状态执行 standard init，补齐最小 `docs/ai` 事实后 strict check 通过；未启用 Workstream，未修改论文、脚本或数据文件。
- 完成三项目统一审查后的修复闭环：knowledgeConnector 删除重复旧回写协议并提交；papers 修正中文原始资料命名规则冲突；Real_Project_Upgrade_Playbook 补充审查包 diff 覆盖和 PowerShell UTF-8 输出要求；`acf upgrade --json` 补齐 detected/planned/skipped 字段并升至 v0.0.3.13。

- 修复非 editable 安装态下 `acf version show --json` 误读安装目录 `pyproject.toml` 的问题：版本命令现在优先发现当前源码 checkout，安装态 show 可回退到包元数据；`version set` 要求源码 checkout，避免误改安装目录。版本升至 `v0.0.3.14`，并刷新用户级 uv tool 安装。

---

## 真实项目升级试点验收

### knowledgeConnector

- 路径：`E:\codes\tools\knowledgeConnector`
- 分支：`test/acf-v0.0.3.12`
- 原状态：已有旧 ACF 上下文，缺 `active/Feedback_Inbox.md` 与 `archive/feedback/`
- 执行方式：`acf upgrade docs/ai --json --check-after`
- changed files：`docs/ai/AGENTS dot md`、`docs/ai/reference/System_Manual dot md`、`docs/ai/rules/Project_Rules dot md`、`docs/ai/active/Feedback_Inbox dot md`、`docs/ai/archive/feedback/.gitkeep`
- strict check：通过
- 是否默认启用 Workstream：否
- 是否批量迁移 front matter：否
- 人工审查结论：diff 仅涉及 ACF 上下文结构与升级文档说明，未改业务代码；未创建 `active/Workstreams.md`
- 后续动作：试点分支已有提交 `0b06452 Upgrade ACF context to v0.0.3.12`，可由项目维护者审查后决定是否合并

### register

- 路径：`E:\codes\tempCodes\register`
- 分支：`test/acf-v0.0.3.12`
- 原状态：已有 ACF standard 上下文，普通 check 与 strict check 均通过
- 执行方式：`acf upgrade docs/ai --dry-run --json`
- changed files：无
- strict check：通过
- 是否默认启用 Workstream：否
- 是否批量迁移 front matter：否
- 人工审查结论：v0.0.3.12 对该项目无新增落盘变更，git diff 为空
- 后续动作：无需升级提交；可继续用现有上下文

### papers

- 路径：`E:\Codes\papers`
- 分支：`test/acf-v0.0.3.12-init`
- 原状态：无 ACF 上下文，根目录无 `AGENTS.md`
- 执行方式：`acf init docs/ai --profile standard --json --check-after`，随后补齐最小项目事实以消除模板占位符
- changed files：新增根 `AGENTS.md` 与 `docs/ai` 标准上下文
- strict check：通过
- 是否默认启用 Workstream：否
- 是否批量迁移 front matter：否
- 人工审查结论：init 只新增 ACF 上下文和薄入口；未修改论文、脚本、数据或业务资料；标准 init 原始模板会在 strict 下暴露占位符，需要最小事实填充后再作为正式上下文使用
- 后续动作：由项目维护者审查新增上下文；后续可逐步完善 `reference/Sources_Index.md`，不要一次性搬运全部历史

---

## 三项目 ACF v0.0.3.12 统一审查

本轮对 knowledgeConnector、register 和 papers 的 ACF v0.0.3.12 试点结果进行了统一审查。

结论：

1. knowledgeConnector 验证了旧 ACF 上下文 upgrade 主路径：strict check 通过，二次 upgrade dry-run 无变更，未默认启用 Workstream，未批量迁移 front matter。审查发现 `docs/ai/AGENTS dot md` 残留重复旧版会话结束回写建议块，已在试点分支提交 `9018be9 Remove stale ACF writeback checklist` 修复。
2. register 验证了 no-op upgrade 主路径：strict check 通过，upgrade dry-run 无变更，工作树干净，当前 Task_Plan 与 Current_Task 一致。
3. papers 验证了新项目 init 主路径：补最小事实后 strict check 通过，upgrade dry-run 无变更，未启用 Workstream。审查发现默认命名规则与中文原始资料文件名冲突，已修正为 ACF 结构文件和新增工具文件优先英文，既有论文、通知和原始资料保留原名。

审查还发现三个 ACF 后续改进方向：

1. 真实项目审查包应覆盖已提交 upgrade 的 `git show` 和新 init 的 untracked 文件内容，避免 diff 层面误判为空。
2. `acf upgrade --dry-run --json` 应与设计契约对齐，补充 `detected_features`、`planned_changes`、`skipped_changes`，或修正文档。
3. PowerShell 审查包脚本需要增强 UTF-8 输出，避免中文 JSON 命令结果 mojibake。

本轮没有发现 Workstream 被默认启用、front matter 批量迁移或业务代码被修改的问题。

---

## v0.0.3.13 发布结论

`v0.0.3.13` 是当前推荐的真实项目接入/升级版本，已通过旧上下文升级、已合规上下文 no-op upgrade、新项目 init 三类真实项目试点。`upgrade` 仍应 dry-run first，Workstream 仍保持显式启用。

`acf check --strict` 只能证明结构、断链、状态和索引一致性；不能证明项目事实完全正确。升级后仍需人工或 AI 审查 Context、Project_Brief、Tech_Context、AGENTS 和项目特有规则是否准确。

当前 tag `v0.0.3.13` 为本地 tag；如需发布到远端，应显式执行 `git push` 和 `git push origin v0.0.3.13`。

---

## 今日讨论 / AI 协作

- 通过 `acf.py new worklog` 生成本工作记录，并更新 `worklog/Worklog_Index.md`。

---

## 有价值的结论

- Workstream 应定位为可选并行目标线协作契约，不是 agent runtime；轻量 front matter 适合新增状态型文档局部试用，不适合全量迁移现有叙述型文档。
- Workstream 详情文件比 Task_Plan 更适合承载并行目标线的边界、读取范围、写入范围、合并请求和状态流转。
- ReadyToMerge 合并请求需要包含目标文件、候选变更、人工判断点、冲突和验证结果，才能让主 agent 不读全过程也能合并。
- Front matter parser 应保持独立于 Workstream；schema 由 Workstream、ADR、Knowledge 等调用方传入，parser 只负责极小 YAML 子集和稳定正文保留。
- Workstream 与 front matter 的兼容策略必须 optional first：旧项目、旧 ADR、旧 worklog、旧 Knowledge 没有新结构或 metadata 时不应被 upgrade/check 打扰。
- Workstream CLI 实现前应先按规格测试命令契约，避免先写命令再返工状态转换、scope 冲突和 optional check 触发条件。

如果这些结论已经成为当前事实，请同步更新到 `active/Context.md`。

---

## 重要决策候选

- ADR-0005：使用可选 Workstream 层管理并行目标线，当前状态为 Proposed；T004 实现验证后再评估是否改为 Active。

如果确认生效，请同步更新到：

- `reference/Decisions_Index.md`
- `decisions/`

---

## 无效尝试

- 无。

---

## 新发现的问题

- 无。

---

## 原始日志位置

- 无。

注意：不要把原始日志全文写入本文件。

---

## 需要同步更新的文件

- `active/Context.md`：视情况更新。
- `reference/Decisions_Index.md`：视情况更新。
- `rules/`：视情况更新。
- `archive/`：视情况归档。

---

## 下一步候选

- 无。

注意：下一步候选不等于当前任务。  
如果某个候选下一步被选为当前任务，请写入 `active/Current_Task.md`。
