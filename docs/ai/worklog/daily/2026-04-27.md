本文件记录当天整理后的项目工作记录。

请注意：本文件是历史过程记录，不是当前事实源。  
当前事实请查看 `active/Context.md`。

---

## 今日完成

- 实现 acf.py new worklog、引入 uv dogfooding 环境，并实现 acf.py new adr。
- 清理 minimal 实例中的 ADR/worklog 占位模板残留。
- 实现 acf.py new task，用 CLI 生成和重置当前任务文件。
- 修复 `acf.py init`，让初始化同时生成缺失的根薄入口 AGENTS.md。
- 新增 `ADR-0003` 记录两层 AGENTS.md 入口结构，修复此前文档中误指向 `ADR-0002` 的编号问题。
- 同步 template 设计理念和维护规则：通用模板需求进入 `template/`，dogfooding 专属事实留在根入口、`docs/ai/` 或维护文档。

---

## 今日讨论 / AI 协作

- 通过 `acf.py new worklog` 生成本工作记录，并更新 `worklog/Worklog_Index.md`。
- 通过 `acf.py new adr` 生成 `ADR-0002`，并更新 `reference/Decisions_Index.md`。
- 将 `init --profile minimal` 和 `simplify` 调整为不再复制 ADR 模板文件与 daily worklog 模板文件。
- 将 `simplify` 调整为复制真实 ADR 和 daily worklog 文件，避免索引断链。
- 移除 `check --strict` 对上述模板文件的占位符白名单，让 strict 对真实实例中的占位符残留保持一致报错。
- 通过 `acf.py new task` 生成本轮当前任务，并设置 Active 任务覆盖需要 `--force`。
- 通过 `acf.py new adr` 生成 `ADR-0003`，记录根薄入口 + 上下文完整入口的两层设计。
- 将 `../Automation.md` 中的根薄入口生成从待办改为已自动化。
- 在 `rules/Project_Rules.md` 中加入后续修改优先使用 `new task`、`new adr`、`new worklog` 和 check 规则的约束。

---

## 有价值的结论

- worklog 和 ADR 的创建及索引更新已进入确定性 CLI 流程，Python 验证入口统一为 uv run python。
- Current_Task 的创建和重置已进入确定性 CLI 流程。
- init 的根薄入口生成已进入确定性 CLI 流程，已有根入口默认不覆盖。
- minimal 真实实例不再保留 ADR/worklog 占位模板文件，真实条目通过 `new adr` 和 `new worklog` 生成。
- `simplify` 应保留真实历史文件，但不保留产品模板占位文件。
- `template/` 是产品模板源，不应吸收本仓库 dogfooding 专属事实；后续模板变更应先判断是否具备通用性。

如果这些结论已经成为当前事实，请同步更新到 `active/Context.md`。

---

## 重要决策候选

- 已解决：minimal 初始化曾残留两个产品模板占位文件，并依赖 strict 白名单放行。
- 已记录：两层 AGENTS.md 入口结构已写入 `ADR-0003`。

如果确认生效，请同步更新到：

- `reference/Decisions_Index.md`
- `decisions/`

---

## 无效尝试

- 无。

---

## 新发现的问题

- 无。

---

## 原始日志位置

- 无。

注意：不要把原始日志全文写入本文件。

---

## 需要同步更新的文件

- `active/Context.md`：已更新。
- `active/Current_Task.md`：已更新。
- `reference/Decisions_Index.md`：已更新。
- `rules/`：已更新 `rules/Project_Rules.md`。
- `../Automation.md`：已更新。
- README、template 入口说明和 System Manual：已同步。
- `archive/`：无需归档。

---

## 下一步候选

- 实现 `acf.py new source`。
- 设计 `acf.py writeback draft` 的输入格式。

注意：下一步候选不等于当前任务。  
如果某个候选下一步被选为当前任务，请写入 `active/Current_Task.md`。
