本文件记录当天整理后的项目工作记录。

请注意：本文件是历史过程记录，不是当前事实源。  
当前事实请查看 `active/Context.md`。

---

## 今日完成

- 删除旧 CLAUDE.md 入口，统一使用 `AGENTS.md`。
- 修复模板中的乱码、空规则文件、示例事实混用和 worklog 断链问题。
- 实现 `acf.py` 的 `init`、`simplify` 和 `check` 命令。
- 添加 CLI 单元测试。
- 新增 `../Automation.md`，记录自动化和 subagent 路线。
- 使用 `acf.py init docs/ai --profile minimal` 初始化本仓库 dogfooding 上下文。

---

## 今日讨论 / AI 协作

- 确认严格 dogfooding 不应复用 `template/` 作为当前事实源。
- 确认真实实例应放在 `docs/ai/`，而不是把 `active/`、`rules/`、`reference/` 直接铺在仓库根目录。
- 确认根 `AGENTS.md` 采用薄入口转发模式。
- 确认当前阶段使用 minimal profile，以降低维护成本。

---

## 有价值的结论

- `template/` 是产品模板，`docs/ai/` 是本仓库当前事实源。
- 确定性 CLI 应优先自动化结构生成、完整度检查和索引一致性检查。
- subagent 后续适合产出草案和审阅意见，不应默认静默写入权威上下文。
- `--strict` 与模板文件共存的语义需要后续设计。

如果这些结论已经成为当前事实，请同步更新到 `active/Context.md`。

---

## 重要决策候选

- 已确认并写入 ADR：使用 `docs/ai` minimal 实例进行 dogfooding，并保持根 `AGENTS.md` 为薄入口。

如果确认生效，请同步更新到：

- `reference/Decisions_Index.md`
- `decisions/`

---

## 无效尝试

- 暂无。

---

## 新发现的问题

- 当前 minimal 初始化仍会复制带占位符的模板文件，真实项目上下文暂不能用 `--strict` 作为验收标准。
- 后续需要设计 `new worklog`、`new adr`、`new task`、`new source` 和 `writeback draft` 的最小接口。

---

## 原始日志位置

- 无。

---

## 需要同步更新的文件

- `active/Context.md`：已更新。
- `reference/Decisions_Index.md`：已更新。
- `rules/`：无需更新。
- `archive/`：无需归档。

---

## 下一步候选

- 改进 `acf.py` 的实例化语义，让真实上下文可以通过 `--strict`。
- 实现 `acf.py new worklog`。
- 实现 `acf.py new adr`。
- 实现 `acf.py writeback draft`。

注意：下一步候选不等于当前任务。  
如果某个候选下一步被选为当前任务，请写入 `active/Current_Task.md`。
