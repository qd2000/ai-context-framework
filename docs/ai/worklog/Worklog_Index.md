本文件是工作记录索引。

请注意：

- 这里不是当前事实源。
- 当前事实请查看：[active/Context.md](../active/Context.md)
- 每日工作记录位于：`worklog/daily/`

---

## 工作记录

| 日期 | 摘要 | 关键结论 | 详情 |
|---|---|---|---|
| 2026-09-20 | WS016 第 3 步推进：抽出共享 argparse helper（cli_arguments.py）与常量（DEFAULT_STALE_DAYS、VALID_FEEDBACK_STATUSES、VALID_HUMAN_INDEX_STATUSES）到叶子模块，并把 13 个命令组的解析器构造下移到各自命令模块；runtime.py 1988 -> 1816 行。; 追加：WS016 第 3 步续：再下移 knowledge 与 edit 两组解析器（累计 15 个命令组），VALID_KNOWLEDGE_STATUSES 移入 constants.py；runtime.py 至 1742 行。; 追加：WS016 第 3 步完成：把 build_parser 剩余全部命令组下移为 register_*_parser，runtime.py 1988 -> 838 行，命令树快照 210 条路径零漂移，全套门禁通过。; 追加：WS016 第 4 步完成：acf.py 不再触碰 runtime 私有全局，改走公开 runtime.set_root 且仅在 ROOT 真正变化时同步；审计脚本排除 dunder 协议属性误报。; 追加：WS016 第 5 步完成：版本收敛 v0.0.3.96、ADR-0007、Architecture 组合契约章节、六域矩阵第 3 域同步，全套门禁 704 项全绿。 | parser composition 的关键不是搬运代码而是保持 handler 身份：CLI 绑定的是 runtime_parts 的 deps 适配器，所以 register_*_parser 必须显式注入 handler；命令树快照护栏（210 条路径的 options/dest/nargs/default/choices + handler 限定名）是唯一能可靠发现这类漂移的手段，本次它确实第一时间抓到了 4 处。; 追加：下移是逐组可验证的机械操作，但每组都必须显式注入 runtime_parts 的 deps 适配器作为 handler；命令树快照让每组只需 0.2 秒即可确认零漂移，整轮行为验证仍由 test_cli 与杂项批次兜底。; 追加：命令组下移的真正约束不是代码搬运而是 handler 归属：CLI 绑定 runtime_parts 的 deps 适配器，所以每个 register_*_parser 都必须显式注入 handler，快照护栏是唯一能可靠发现漂移的手段；对超大命令组（workstream 29 个 handler、continuation 顶层组）改为独立 *_parsers 模块并做缺失 key 校验，避免把已接近 2000 行门禁的命令模块推过去。; 追加：顶层 shim 的“同步”本质是把 ROOT 变化传播给被注入的 part 模块；把它收成一个公开、幂等的 set_root 入口后，shim 只需判断差异再调用，既保留 legacy 契约又彻底切断对私有全局与私有同步函数的依赖，同时可用源码断言把“不写内部”固化为契约。; 追加：权威文件（decisions/*.md、reference/Decisions_Index.md）的写 gate 已经收紧到 merge_targets + merge request，不再接受 WS014 时代 assigned: decisions/ 的声明；写 ADR 类权威内容时应直接走该治理路径，否则 strict check 会给出明确指引。 | `worklog/daily/2026-09-20.md` |
| 2026-09-19 | v0.0.3.95 已成功发布到 PyPI（latest=0.0.3.95），发布链闭合：定位到 v0.0.3.93/.94 发布失败的共同根因是 WS014 新引入的 release.yml verify 门禁在 Windows runner 上暴露的 3 处既有测试夹具缺陷（8.3 短路径别名 + 终止进程句柄瞬时占用），修复并重发。; 追加：WS016 第 1 步完成：runtime 与 runtime_parts 的 __dict__ 双向合并收窄为显式导出 allowlist（新增 runtime_exports.py，202 个必需导出名），四种违约情形 fail-closed；新增 runtime_export_audit.py 与 9 项契约回归。; 追加：WS016 第 2 步完成：新增有界 RuntimeContext（82 + 13 名），workstream 消费侧不再是全量命名空间拷贝，28 处 per-call _bind 改为一次性冻结绑定。; 追加：WS016 第 3 步（parser composition）先交付命令树形状快照护栏：新增 tests/test_cli_surface_snapshot.py 与 67KB 生成快照 tests/fixtures/cli_surface.json，锁定每个子命令的 handler 与全部 action 的 options/dest/nargs/default/choices。 | 发布门禁失败的根因是 Windows hosted runner 的 8.3 短路径别名（RUNNER~1 vs runneradmin）让 overlap 竞态注入静默失效（在 runner 上该用例空跑并误判合并被允许）、install 脚本用例做了裸路径字符串比对、以及 tearDown 的临时目录被刚终止进程瞬时占用；三处均属测试夹具缺陷，产品行为不变，已用本机 GetShortPathNameW 构造别名条件复现验证。; 追加：runtime 公开命名空间从 769 降到 531、runtime.py 从 1988 降到 1979 行，且全量门禁（689 项 unittest + check template/strict + smoke 8/8 + upgrade matrix 28 fixtures）保持全绿，证明这是零行为变更的内部重构；期间发现并显式保留了 archive_workstream.__getattr__ 这条 PEP 562 惰性转发依赖。; 追加：把 Workstream handler 每次调用都拷贝数百个名字到模块 globals 的开销与隐式面同时消掉：注入面 531 -> 82/13，且 _bind 的刷新与遮蔽语义未变；全量 692 项 unittest 与 check/smoke/upgrade matrix 保持全绿，证明仍是零行为变更的内部重构。; 追加：护栏就位但下移尚未开始：内联解析器块依赖 runtime 局部 helper（add_write_arguments/validate_date/validate_draft_name/DEFAULT_STALE_DAYS），与既有 register_*_parser 签名不匹配；必须先抽共享 argparse helper 才能保持既有注册形状并让 runtime.py 真正减行，因此按计划顺序把它登记为下一步而非现场改签名。 | `worklog/daily/2026-09-19.md` |
| 2026-09-18 | WS014 稳定化与产品健康治理：authority 文档收敛、smoke 运行态隔离、孤儿日志 GC、issue 独立生命周期与首轮 triage、owner-context 隔离验证、ADR-0006 与 CI 收口。; 追加：WS015 发布链收口与 worktree 退役窄路径：登记 WS015 目标线；诊断 v0.0.3.93 tag 已推送但 PyPI 未发布的发布链；实现 acf worktree retire 窄路径与 11 项回归；同步 Worktree_Lifecycle、双份 System Manual、Automation、README、六域 Gap Matrix、Roadmap 与 CHANGELOG；版本收敛到 v0.0.3.94 并通过全套本地门禁。 | 机械检查全绿且测试运行不再污染真实运行态；产品 issue 由 9 条 open 收口为 4 条，关闭不再依赖 active continuation。; 追加：发布链失败不在代码与制品层（本地 v0.0.3.93 wheel+sdist 可构建且可隔离安装）；curated handoff 退役只移除 worktree 与 registry、永不删除分支，且 close 的 branch_not_merged 硬门保持不变；v0.0.3.94 的 PyPI 发布与全局安装仍需用户明确授权后才能执行。 | `worklog/daily/2026-09-18.md` |
| 2026-08-06 | 重写 Worktree 合并韧性计划，完成 T001 合同与测试夹具，并启动 T002 结构化 Git 状态快照。 | 所有 merge 统一使用临时 integration worktree；primary 只做 fast-forward promotion；ignored artifact 在 promotion 前处理。376 项全量回归通过。 | `worklog/daily/2026-08-06.md` |
| 2026-08-04 | 完成ACF Workstream与Git Worktree生命周期开发和发布验证; 追加：WS005已合入master并升级生产ACF v0.0.3.54 | v0.0.3.54全部功能和353项测试通过，待合并master后升级生产uv tool。; 追加：生产acf.exe完整生命周期smoke通过，开发worktree可安全关闭。 | `worklog/daily/2026-08-04.md` |
| 2026-06-09 | 清理外部复审后的上下文卫生：F016 标记完成并归档，F001-F014/F016/F018 移入 archive/feedback/2026-06.md，F015/F017 回到真实 Triaged 状态，旧 Knowledge 草案移入 archive/knowledge-drafts，并修正文档安装路径和版本示例。验证：acf check --strict、acf check template、review stale、feedback archive-candidates、version show、git diff --check。 | 无。 | `worklog/daily/2026-06-09.md` |
| 2026-05-30 | 实现 Workstream ReadyToMerge 人工确认硬门禁：acf workstream ready 默认拒绝未带 --human-approved 的状态迁移，新增 workstream_human_approval_required 错误码和 next_actions；带 --human-approved 后继续保留原有状态、合并请求和未完成 stage 检查。同步 README、模板 System_Manual、dogfooding System_Manual 和 Workstream_Design，并更新测试覆盖。验证：uv run acf check template、uv run acf check --strict、uv run python -m unittest、全局 acf workstream ready WS020 --dry-run --json 均符合预期。 | 无。 | `worklog/daily/2026-05-30.md` |
| 2026-05-29 | 归档测试用 WS001 并记录多 agent 多 Workstream 使用体验反馈。 | WS001 已移出 active，Workstream 层回到 Inactive；并行使用体验需要后续从产品设计上降低操作摩擦。 | `worklog/daily/2026-05-29.md` |
| 2026-05-09 | 固化 human 层索引治理并发布 v0.0.3.44; 追加：完成 Workstream 强隔离治理：新增 context/guard/scope-add/dashboard/merge-start、Task/Merge/Maintenance 类型、Merging 状态、shared/owned 门禁和 docs/template 同步；版本提升到 v0.0.3.45。验证：235 个 unittest、docs/ai strict check、template check、version show、workstream dashboard。 | human/ 保留为 context root 下的人类半结构化输入层；新增 Human_Index、acf human index/list/mark、strict path check 和升级补齐能力，已通过模板检查、strict check、单元测试和 minimal smoke。 | `worklog/daily/2026-05-09.md` |
| 2026-05-08 | 完成 Top-Level Design Implementation Gap Audit：新增 Top_Level_Implementation_Gap.md，将顶层设计映射到当前实现，识别 JSON contract consistency、Workstream stage flow fixture、upgrade matrix expansion 为下一批 P1 实施切片；更新 Product_Roadmap、Sources_Index、Task_Plan 和 Current_Task。验证：uv run acf check docs/ai --strict --json；uv run acf audit context docs/ai --json。; 追加：完成 P1 JSON contract consistency tests：新增 AI-facing JSON 成功/失败最小契约 helper 和代表性命令覆盖；为 check/status --json 失败 payload 补 message 字段；版本 bump 到 v0.0.3.28。验证：unittest、check template、docs/ai strict check、upgrade dry-run、version show、minimal_smoke、upgrade_matrix quick。; 追加：完成 P1 Workstream stage flow fixture：新增 tests/fixtures/context_matrix/workstream_stage_flow，并在 [tests/test_context_matrix.py](../../../tests/test_context_matrix.py) 覆盖 stage add/list/focus/done、dependency blocked、multi Active conflict、missing evidence、clear-current required、strict check、sync no-op 和 audit clean；minimal_smoke 明确断言 workstream sync dry-run changed_files=[] 与 audit candidates=[]；Top_Level_Implementation_Gap 和 Product_Roadmap 已标记 P1-1/P1-2 完成，下一入口为 P1-3 upgrade matrix expansion。验证：unittest、check template、docs/ai strict check、audit context、upgrade dry-run、minimal_smoke、upgrade_matrix quick。; 追加：完成 P1 Upgrade matrix expansion：[scripts/upgrade_matrix.py](../../../scripts/upgrade_matrix.py) 增加 expected_absent、detected_features、changed_files suffix 和 Workstream sync no-op 断言；新增 old_without_workstreams_optional、old_workstreams_without_current_stage、old_adr_without_front_matter、custom_agents_missing_reference_combo 四个 fixture；[tests/test_upgrade_matrix.py](../../../tests/test_upgrade_matrix.py) 显式断言新增 quick/full fixture。Top_Level_Implementation_Gap 与 Product_Roadmap 已标记 P1-1/P1-2/P1-3 完成，下一入口转入 P2 级评估。验证：unittest、check template、docs/ai strict check、audit context、upgrade dry-run、minimal_smoke、upgrade_matrix quick/full。; 追加：启动 P2 Task Stage CLI：P1 gap-driven 基础切片已完成，当前进入 Task Stage CLI 薄切片；范围限定为维护 [active/Task_Plan.md](../active/Task_Plan.md) 的 ## 任务阶段 表，不改变 Current_Task、不自动激活 Workstream、不新增 audit 规则。; 追加：完成 P2 Task Stage CLI 薄切片：新增 acf plan stage list/add/set/done，只维护 [active/Task_Plan.md](../active/Task_Plan.md) 的 ## 任务阶段 表；校验阶段 ID 归属父任务、父任务存在和 optional Workstream owner 存在；done 要求 evidence；未创建 task object 单文件，未自动修改 Current_Task，未联动 Workstream。新增 task_stage_registry fixture、CLI 单元测试和 minimal_smoke Task Stage 主路径；同步 README、System_Manual、Automation、Top-Level Design、Product Roadmap、Gap 文档并 bump 到 v0.0.3.29。验证：unittest、check template、docs/ai strict check、audit context、upgrade dry-run、version show、minimal_smoke、upgrade_matrix quick/full、py_compile、git diff --check。; 追加：启动 P2 Workstream lifecycle/archive helper 设计：上一阶段 Task Stage CLI 已提交推送且工作树干净；本阶段先补 archive helper 边界和 synthetic fixture，不直接实现会移动文件的 workstream archive 命令，不新增 audit 高风险规则，不 bump 版本，除非后续实际改变 CLI 行为。; 追加：完成 P2 Workstream lifecycle/archive helper 设计与 fixture：新增 Workstream_Lifecycle_Archive_Design.md，明确第一版先做只读 archive-candidates 或 archive draft，不直接移动文件；新增 workstream_lifecycle_archive context_matrix fixture，覆盖 retained terminal Workstream strict clean、terminal Workstream 作为当前执行线被拒绝、expired keep-active strict error；同步 Sources_Index、System_Manual、Workstream_Design、ACF_Top_Level_Design、Product_Roadmap、Top_Level_Implementation_Gap 和 active Context。本轮未改变 CLI 行为，不 bump 版本。验证：targeted context_matrix tests、unittest、docs/ai strict check、audit context、check template。; 追加：开始 P2-3 Workstream archive-candidates：上一阶段已提交 bcca7e4；本阶段按顶层设计只实现只读候选发现，不移动 active/workstreams 文件。; 追加：P2-3 已实现只读 workstream archive-candidates：输出 candidates/blocked/blocked_by/changed_files=[]；补 CLI 单测、context_matrix lifecycle 断言、minimal smoke、README/System Manual/roadmap/gap 文档，并 bump 到 v0.0.3.30。; 追加：P2-3 验证通过：187 个 unittest、minimal_smoke、py_compile、template check、upgrade_matrix quick、docs/ai strict check、audit context、upgrade dry-run 和 diff --check 均通过。; 追加：补齐 active 后续规划：上一阶段 P2-3 已完成并提交 5cea082；当前 active 计划切到 P2-4 Workstream Archive Draft and Index Cleanup Design，T001 为设计 archive draft/index cleanup 边界。; 追加：完成 P2-5 路线校准：将下一代码切片收敛为 context_matrix audit fixture expansion，优先补 audit_stale_stage 和 audit_terminal_merge fixtures；Knowledge/ADR/Archive sync 先设计 generated marker，curation draft 增强后置。; 追加：完成 P2-6 human layer、Obsidian 边界、统一占位符和 canonical ACF marker：standard init/upgrade 维护 human 层，minimal 不补；upgrade 兼容旧 marker 并对旧格式给 warning；template 占位符迁移到 ACF-keyed form；docs/ai 通过新 upgrade 补出 human 层并更新 AGENTS。验证通过：check template、check docs/ai --strict、audit context、py_compile、unittest、minimal_smoke、upgrade_matrix quick、git diff --check。 | 无。 | [worklog/daily/2026-05-08.md](daily/2026-05-08.md) |
| 2026-05-07 | 使用 fcc_workspace 完成 P0 strict 修复后回归：FCC WS002 已补 merge_resolution=merged、keep_active_reason、keep_active_until；WS004 已移除 write_scope 中对 Context.md / Current_Task.md 的 direct assigned claim，改用 merge_targets 和机器可读合并请求。Windows 侧当前 ACF v0.0.3.26 strict check 与 FCC WSL/wuv strict check 均通过；workstream sync dry-run 仍提示 [active/Workstreams.md](../active/Workstreams.md) 可进一步同步索引行，但未自动应用以避免重写索引说明文本。FCC 提交：1f1c32e Satisfy P0 Workstream strict gates。; 追加：完成 P1 audit context 设计草案，明确 audit 只输出 candidates、summary 和 next_actions，不进入默认 strict，不自动修改事实；新增 Context_Audit_Design，并更新 Automation、Task_Plan 和 Current_Task。验证：uv run acf check docs/ai --strict --json；uv run acf status docs/ai --json。; 追加：完成 T002 audit MVP command contract 设计复核：冻结 acf audit context [path] --json 的只读命令边界、顶层 JSON 字段、candidate 字段和 summary 结构；MVP 首批规则收窄为 active_section_too_long、stale_current_task_or_workstream_stage、terminal_conclusion_not_merged；duplicate_active_fact_candidate、strong_claim_without_evidence、volatile_fact_in_wrong_authority_location 暂缓。验证：uv run acf check docs/ai --strict --json；uv run acf status docs/ai --json。; 追加：实现 T003 audit context MVP：新增只读 acf audit context [path] --json，输出 candidates、summary、next_actions；MVP 规则限定为 active_section_too_long、stale_current_task_or_workstream_stage、terminal_conclusion_not_merged；未实现 duplicate fact、strong claim without evidence、volatile fact wrong location、patch/fix/curation draft 或 strict 接入。验证：targeted audit tests、py_compile、unittest、check template、docs/ai strict check、status、dogfooding audit context。; 追加：完成 P1 audit context MVP dogfooding pass：ACF 自身 audit context candidates=[] 且 strict check clean；在 Windows 宿主机路径 E:\Codes\fcc_workspace\docs\ai 上只读运行 audit/check，未使用 WSL、未修改 FCC。FCC strict check clean；audit 输出 4 个 active_section_too_long candidates，集中在 active/workstreams/WS008.md、WS009.md、WS010.md；未出现 stale_current_task_or_workstream_stage 或 terminal_conclusion_not_merged 候选。初步判断 MVP 输出不吵，path/section/reason/suggested_action 足以指导后续人工或 AI 整理；下一步可考虑 T004：调优 long-section message 或阈值，但暂不扩展 duplicate/evidence/volatile 规则。; 追加：启动并完成 T004 audit MVP candidate quality review：ACF 自身 audit clean；FCC Windows 宿主机只读 audit 输出 4 个 active_section_too_long candidates，集中在 WS008/WS009/WS010，未观察到 stale 或 terminal merge 误报。结论：当前 MVP 输出不吵，字段足以指导后续复核；下一步应优先评估 long-section 阈值和 suggested_action 质量，暂不扩展 duplicate/evidence/volatile 规则。; 追加：完成 T005 active_section_too_long 调优：FCC 只读复核确认 4 个 long-section candidates 中 3 个来自 Workstream H1 wrapper 计数；规则改为跳过带子标题的 H1 wrapper，保留真实长 section 检查，并改进 suggested_action/next_actions。调优后 FCC audit 从 4 个 candidates 降为 1 个 WS009 分析层级候选；ACF 自身 audit 仍为 candidates=[]。本轮未新增 audit 规则，未修改 FCC，未接入 strict。; 追加：完成 P2 generalization plan 文档收口：新增 [reference/Product_Roadmap.md](../reference/Product_Roadmap.md)，明确 FCC 是压力测试样本而非产品需求唯一来源；真实项目反馈进入 ACF 前必须抽象为通用上下文治理问题，并满足可机械检测、可测试、可兼容、可降级等准入门槛。同步 Automation、Project_Brief、Sources_Index、Task_Plan 和 Current_Task。下一步候选从继续 FCC-driven audit 扩规则切换为 P2 Test Matrix Expansion：补 minimal/legacy/complex workstream/audit long section/authority gate fixtures，并选择非 PetroSim 验证样本。; 追加：完成 P2 Test Matrix Expansion 初版：盘点确认现有 tests/fixtures/upgrade_matrix 覆盖 legacy/upgrade 兼容；新增 tests/fixtures/context_matrix 与 [tests/test_context_matrix.py](../../../tests/test_context_matrix.py)，覆盖 minimal_clean、legacy_old_context、audit_long_section、workstream_complex、authority_gate。测试验证 minimal audit clean、H1 wrapper 不误报且真实长 H2 section 会报、complex Workstream strict 与 sync no-op、direct authority claim strict fail。结论：P3 前仍需要选择一个非 PetroSim 真实项目样本；本轮未新增 audit rule，未修改 acf.py，未修改 FCC。; 追加：完成 P2 Non-PetroSim Dogfooding Sample 选择与只读评估：选择 E:\Codes\EcSOS\cracking-yield-prediction-system 作为非 PetroSim 真实样本。该项目是论文/数据分析/Python 工具项目，已有旧版 ACF context 和 Active 当前任务，不依赖 PetroSim runtime，适合验证非 Workstream-heavy 项目的 upgrade/audit 适用性。只读结果：目标 git status 显示 ahead 1；acf status/check/strict 当前因缺少 [reference/Context_Curation_Prompt.md](../reference/Context_Curation_Prompt.md) 失败；upgrade --dry-run 计划补 Context_Curation_Prompt 并更新 AGENTS、Project_Rules、System_Manual；audit context 输出 1 个 stale_current_task_or_workstream_stage candidate；review stale 输出 Current_Task 缺 dated status note 与 Context 缺 review marker 两个 stale items。本轮未修改 EcSOS，未新增 audit rule。; 追加：完成 EcSOS upgrade rehearsal plan：记录 EcSOS ahead 1 的具体本地提交 c236a77 初始化docs/ai/；明确正式写入前需复核 git status、ahead commit 和 upgrade dry-run planned changes。计划约束：无用户授权不写 EcSOS；upgrade apply 仅限结构文件 [reference/Context_Curation_Prompt.md](../reference/Context_Curation_Prompt.md)、AGENTS.md、[rules/Project_Rules.md](../rules/Project_Rules.md)、[reference/System_Manual.md](../reference/System_Manual.md)；写后只验证 check/audit/review stale/git diff，不自动修 stale 当前任务、Context review marker 或论文事实。本轮未修改 EcSOS，未新增 audit rule。; 追加：完成 EcSOS upgrade apply 演练：前置复核确认目标项目 master 与 origin/master 无分叉，HEAD 为 c236a77 初始化docs/ai/；upgrade dry-run planned changes 仍限于 [reference/Context_Curation_Prompt.md](../reference/Context_Curation_Prompt.md)、AGENTS.md、[rules/Project_Rules.md](../rules/Project_Rules.md)、[reference/System_Manual.md](../reference/System_Manual.md)。已按授权执行 uv run acf upgrade E:\Codes\EcSOS\cracking-yield-prediction-system\docs\ai --json，实际 changed_files 与 dry-run 一致。写后验证：EcSOS acf check 与 strict check 均通过；audit context 保留 1 个 stale_current_task_or_workstream_stage candidate；review stale 保留 Current_Task 缺 dated status note 与 Context 缺 review marker 两个候选。未修 stale，未改论文事实，未运行训练/采样/runtime。观察：Project_Rules.md 产生一条相近重复规则，作为后续 upgrade 幂等/去重评估样本保留，未手工修。; 追加：EcSOS post-upgrade read-only verification passed：strict check clean；audit context 保留 1 个 stale_current_task_or_workstream_stage candidate；review stale 保留 Current_Task dated note 与 Context review marker 两个候选。EcSOS git status clean 且与 origin/master 同步。结论：EcSOS 结构 upgrade 已闭环，剩余项属于项目语义治理 / 上下文整理，不是 upgrade 问题，也不应立即转成 ACF 产品规则。; 追加：完成 P2 Workstream Task Flow：新增 acf workstream stage add/list、workstream focus、workstream stage done；阶段注册只改详情文件，focus 拒绝多 Active/终态/同 WS 依赖未 Done，stage done 要求 evidence 且完成 current_stage 需 --clear-current；strict check 新增 Done stage evidence 检查。同步 Workstream_Design、README、System_Manual、模板手册和版本 v0.0.3.26。验证：unittest、check template、docs/ai strict check、upgrade dry-run、version show、minimal_smoke、upgrade_matrix quick。; 追加：完成 Workstream Task Flow release 收口：确认本地实现提交 1202ed4 已与 origin/master 同步，origin/master..HEAD 为空；将外部 CLI 行为版本从 v0.0.3.26 提升到 v0.0.3.27，并同步 README、System_Manual、Context 和包元数据；minimal_smoke 的 Workstream happy path 新增 stage add -> focus -> stage done --clear-current 临时 synthetic 项目验证链。验证：uv run python -m unittest、uv run acf check template --json、uv run acf check docs/ai --strict --json、uv run acf upgrade docs/ai --dry-run --json、uv run acf version show --json、uv run python [scripts/minimal_smoke.py](../../../scripts/minimal_smoke.py) --acf uv run acf、uv run python [scripts/upgrade_matrix.py](../../../scripts/upgrade_matrix.py) --mode quick。; 追加：沉淀 ACF 顶层产品设计和阶段路线：新增 ACF_Top_Level_Design.md，重整 Product_Roadmap.md，并同步 Sources_Index、System_Manual 和 Automation 引用；strict check 与 audit context 均通过。; 追加：补强 ACF 顶层设计收口约束：明确设计文档权威关系、JSON/error_code 最小契约、版本 bump 判断、Workstream focus 多 Active stage 拒绝策略，以及 Done/Cancelled 到 archive 的生命周期边界；strict check 与 audit context 均通过。 | 无。 | [worklog/daily/2026-05-07.md](daily/2026-05-07.md) |
| 2026-05-06 | 实现 PR 1b Workstream Stage Focus：支持 optional current_stage 与 Workstream ## 阶段 表检查，覆盖未注册、跨 WS、唯一 Active 和终态状态测试。; 追加：实现 PR 2 Workstream authority write gate + merge_targets：新增 merge_targets metadata，strict 下拒绝 assigned/owned 直接命中内置 authority path，merge-request 同步维护 merge_targets，ReadyToMerge/Done 声明 merge_targets 时要求合并请求。; 追加：完成 PR 3 Merge resolution + active retention gate：Workstream schema 允许 merge_resolution / keep_active_reason / keep_active_until；acf check 会拒绝 Done 缺 evidence 或 merge_resolution、非法 merge_resolution、非法 keep_active_until 和 strict 下已过期的 keep_active_until；terminal active Workstream 缺 keep-active metadata 时普通 check 给 warning；workstream done 现在要求 --merge-resolution，并已同步 smoke 与文档。; 追加：完成 PR 4a Workstream index consistency check：acf check 现在按详情 front matter 作为事实源，检测 Workstreams 索引行与详情 status/title/owner 不一致；普通 check 给 warning，strict check 升为 error。索引行指向缺失详情继续 error，详情存在但索引缺行继续 warning/strict error；Done/Cancelled 索引保持 Inactive 时不触发默认读取。未实现 acf workstream sync，PR 4b 后续单独处理。验证：targeted PR4a tests、unittest、check template、strict docs check、upgrade dry-run、minimal smoke、quick upgrade matrix、py_compile。; 追加：完成 PR 4b acf workstream sync：新增 acf workstream sync [path]，支持 --dry-run --json preview、changed_files、正式写入和 --check-after/--strict。sync 只根据 active/workstreams/*.md front matter 更新 [active/Workstreams.md](../active/Workstreams.md)，修复 status/title/owner/write_scope/depends_on 行并追加缺失详情行；不删除缺详情的旧索引行，不移动 Done/Cancelled 文件，不改 Workstream 详情正文，不碰 Knowledge/ADR/Archive 索引。已用 sync 收敛 docs/ai Workstreams 索引，随后 dry-run no-op。验证：sync targeted tests、unittest、check template、strict docs check、upgrade dry-run、minimal smoke、quick upgrade matrix、py_compile。; 追加：使用 fcc_workspace 验证 P0 governance hardening：ACF 自身 release-ish 验证通过；FCC strict check 抓到 WS002 Done 缺 merge_resolution、缺 keep_active_reason/keep_active_until；workstream sync dry-run 发现 [active/Workstreams.md](../active/Workstreams.md) 可由详情元数据重建；WS004 仍以 [docs/ai/active/Context.md](../active/Context.md) 和 [docs/ai/active/Current_Task.md](../active/Current_Task.md) 形式 claim authority path，但当前 authority gate 未拦截，后续需补 docs/ai 前缀归一化。; 追加：纠正 fcc_workspace 压测执行面：FCC 项目事实源位于 Windows 宿主机 E:\\Codes\\fcc_workspace；WSL/wuv 调用虽访问 /mnt/e 同一目录，但使用的是 FCC WSL 环境中的 acf v0.0.3.25，不适合作为当前 ACF 工作树回归依据。已改用 ai-context-framework 仓库内 uv run acf 直接检查 E:\\Codes\\fcc_workspace\\docs\\ai；复跑 status/check/strict/workstream status/workstream sync dry-run 后，结果与前次一致：WS002 缺 merge_resolution 与 keep-active metadata；WS004 docs/ai 前缀 authority claim 仍未被拦截，T007 继续跟进路径归一化。; 追加：完成 T007 authority path 前缀归一化补强：新增 [docs/ai/active/Context.md](../active/Context.md) 与 docs\\ai\\active\\Context.md claim 的回归测试，并确认 docs/ai/worklog/writeback-drafts/WS004-draft.md 这类非 authority project-root path 不误伤；实现仅新增 authority 匹配专用归一化，先做 slash 归一化，再去掉 docs/ai/ 前缀后匹配内置 authority path。FCC 只读复测已确认 WS004 的 [docs/ai/active/Current_Task.md](../active/Current_Task.md) 与 [docs/ai/active/Context.md](../active/Context.md) assigned claim 现在会被 strict check 拦截。验证：targeted authority tests、py_compile、check template、strict docs check、upgrade dry-run、unittest、minimal smoke、quick upgrade matrix、FCC strict check。; 追加：完成 P0 governance hardening release/readiness 收尾：确认 Task_Plan 大任务 Done、Current_Task Done；完整验证通过 check template、strict docs check、upgrade dry-run、unittest、minimal smoke、quick/full upgrade matrix；因本轮修改 CLI/check 外部行为，将版本从 v0.0.3.25 提升到 v0.0.3.26，version show 确认 cli/pkg_info/pyproject/uv_lock 一致。 | 无。 | [worklog/daily/2026-05-06.md](daily/2026-05-06.md) |
| 2026-05-05 | 完成阶段 6 事实与注意力治理 P0 规则层落地。; 追加：实现 acf review stale 只读机械检查命令。; 追加：补充 Context 文件级审阅标记最小规范。; 追加：增强 acf review stale JSON 输出契约。; 追加：增强 review stale clean 状态和 next_actions。; 追加：实现 curate draft 最小版。; 追加：实现 upgrade compatibility runner。; 追加：新增 Context Curation Prompt 模板。 | v0.0.3.18 将默认目标收敛为维护低噪声、高权威、任务相关的默认注意力入口；P1 review stale / curate draft 仍仅为路线。; 追加：review stale 已覆盖默认注意力入口的 stale candidates 输出；curate draft 仍保持为后续路线。; 追加：review stale 已能识别 Last reviewed / 上次审阅 标记；dogfooding Context 当前不再产生缺少审阅标记候选。; 追加：review stale 现在输出 summary 和统一 stale item 字段；message 兼容字段暂保留，curate draft 仍未实现。; 追加：review stale 现在无候选时明确报告 clean，有候选时按 kind 给出机械下一步建议；仍不裁决语义、不写文件。; 追加：curate draft 现在只消费 review stale 的机械信号生成可审阅治理草案；clean 时不创建空草案，同名草案安全拒绝。; 追加：P1.5 新增风险驱动升级兼容矩阵，quick 随单元测试运行，full 用于发布前扩展检查；矩阵暴露并修复旧 AGENTS/System Manual 二次升级幂等性问题。; 追加：P1.6 将上下文整理 prompt 作为按需 reference 资料接入 init/upgrade/data-files/upgrade_matrix；默认产物是整理建议，不改变 review stale / curate draft 的机械边界。 | [worklog/daily/2026-05-05.md](daily/2026-05-05.md) |
| 2026-05-03 | 实现 acf new worklog --append 的 AI-safe 定点补记能力，补齐稳定 JSON/error_code、dry-run preview、repo-relative POSIX 路径、结构化 warnings 和 anchor 缺失失败契约。; 追加：补充 worklog append anchor 守护测试，确保 render_worklog_daily 与模板示例保留 ## 今日完成 和 ## 有价值的结论。; 追加：完成 v0.0.3.17 第二轮有效性评测补证：本仓库基线、临时新项目主路径、worklog append 分支和 3 个真实项目只读升级/严格检查均通过；KnowledgeConnector 暴露 schema 已齐但内容状态漂移的诊断样本；当前剩余主要缺口是稳定安装入口仍停留在 v0.0.3.16。; 追加：根据用户反馈将下一轮 v0.0.3.17 验证整理为正式任务计划：覆盖稳定安装最小环境、真实项目写入闭环、worklog append/force 边界、跨项目健康/漂移样本和用户旅程摩擦点；旧 Done 计划已归档，新计划聚焦 T001。; 追加：完成 T001 稳定安装验证：全局 acf 已由 v0.0.3.16 更新到 v0.0.3.17；隔离最小标准项目验证 init/status/upgrade dry-run/check 主路径，strict 因模板占位符按预期失败；fcc_workspace、papers、register 使用稳定入口的 status、upgrade dry-run 和 strict check 均通过。; 追加：完成 T002 真实项目轻写入闭环：在 E:\Codes\TempCodes\register 使用稳定入口 acf v0.0.3.17 先 dry-run，再创建并追加 2026-05-03 daily worklog；写后 strict check 通过，目标项目只产生预期 worklog diff；当前计划焦点切到 T003。; 追加：完成 T003 worklog append/force 边界与恢复矩阵：稳定入口在隔离项目中正确返回 TARGET_EXISTS_APPEND_REQUIRED、APPEND_FORCE_CONFLICT、ANCHOR_NOT_FOUND；append 恢复成功，dry-run 和失败分支均不改文件 hash；路径、错误 JSON 和 warnings 契约复核通过；当前计划焦点切到 T004。; 追加：完成 T004 跨项目健康/漂移分类评测：fcc_workspace、papers、register 作为健康样本通过稳定入口只读主路径；KnowledgeConnector 作为 schema 已齐但内容状态漂移的诊断样本，upgrade dry-run no-op，strict check 暴露 27 个一致性错误且无 diff；.acf.lock 观察纳入 T005 摩擦点整理。; 追加：完成 T005 与本轮评测收口：审阅 T001-T004 证据后，将 v0.0.3.17 稳定安装、真实写入闭环、错误码恢复矩阵、跨项目 healthy/diagnostic 分类结论写入 Context；将最小 scenario smoke runner 产品化和 .acf.lock 运行态残留处理作为开放问题保留；大任务计划已标记 Done。; 追加：根据用户反馈补齐 T005 摩擦点决策矩阵，将 T001-T004 证据压缩为决策去向、smoke runner 候选范围和 v0.0.3.17 推荐版本结论；.acf.lock 残留单独进入 Feedback_Inbox F014 作为待分类改进候选。; 追加：根据用户收口建议归档 v0.0.3.17 稳定安装与跨项目评测：先将 F014 后续口径改为 .acf.lock 三分法复现分类，再归档 Current_Task 和 Task_Plan，使 T001-T005 从 active 工作转入 archive 证据。; 追加：完成 F014 .acf.lock 残留复现分类：正常串行读写和正常并发撞锁拒绝后不残留锁；硬中断持锁进程会留下 .acf.lock，因此归类为异常中断运行态副作用，并归档任务与计划。; 追加：实现最小 smoke runner：新增 [scripts/minimal_smoke.py](../../../scripts/minimal_smoke.py)，用隔离临时目录和 CLI JSON 输出覆盖 init -> nested status/check、worklog create/append/error_code、Workstream minimal happy path；同步 README 与 docs/Automation，并归档任务和计划。 | append 能力限定在 daily worklog，并通过 --append 参数与 --force 替换语义区分，避免引入全局 append 语义。; 追加：worklog append 的模板 anchor 依赖已由专门测试显式守护。; 追加：T005 的价值应是把评测证据转成后续决策，而不是只写过程总结。; 追加：v0.0.3.17 评测闭环已归档；active 区域只保留 F014 作为后续待分类反馈。; 追加：F014 不需要立即开 CLI 修复任务；后续只保留可选清理提示或仓库卫生说明。; 追加：最小 smoke runner 只做发布前快速回归，不替代真实项目批量评测、漂移样本诊断或复杂 upgrade 审查。 | [worklog/daily/2026-05-03.md](daily/2026-05-03.md) |
| 2026-05-01 | 补强 usage log 反馈记录能力：确认自动 usage event 只适合记录命令元数据，新增显式 acf log feedback 用于记录实际使用反馈正文，并同步文档、版本和测试。 | 无。 | [worklog/daily/2026-05-01.md](daily/2026-05-01.md) |
| 2026-04-30 | 沉淀 Workstream 设计计划，完成 dogfooding gate、front matter 基础设施、upgrade 兼容矩阵、CLI/check 规格和 ADR 候选 | Workstream 应定位为可选并行目标线协作契约；front matter parser 独立于 Workstream；upgrade/check 必须 optional first；ADR-0005 先保持 Proposed，待 T004 实现验证后再评估 Active。 | [worklog/daily/2026-04-30.md](daily/2026-04-30.md) |
| 2026-04-29 | 实现任务规划、归档、升级、Knowledge 层增强，并完成反馈入口、回写流程、upgrade 兼容性规则和已有文件迁移修复。 | upgrade 不应只补缺失文件；对已存在但内容过期的 ACF 模板文件，需要保守 section 级迁移并用 dry-run/check-after 验证。 | [worklog/daily/2026-04-29.md](daily/2026-04-29.md) |
| 2026-04-27 | 实现 acf.py new task、new source、new worklog、new adr、writeback draft，引入 uv dogfooding 环境，清理 minimal 模板残留，修复 init 根薄入口生成，写入 AI-facing CLI 长期阶段计划，实现阶段 1/2/3，补充 CLI 渐进式披露入口、PATH 安装验收说明、acf edit dogfooding 规则、新增 opt-in 使用状态日志，并修复 Context 后切换到跨项目 dogfooding 评测任务。 | 当前任务、资料索引、worklog、ADR、根薄入口生成、会话回写草案、可安装入口、上下文自动发现、status、JSON 输出、dry-run、changed files、错误分类、section/table 编辑、CLI 发现提示、PATH 安装说明、docs/ai 结构化编辑规则、usage event log 和跨项目 dogfooding 评测任务已进入确定性流程。 | [worklog/daily/2026-04-27.md](daily/2026-04-27.md) |
| 2026-04-26 | 初始化本仓库的 `docs/ai` dogfooding 上下文。 | 采用 minimal 实例和根薄入口，`template/` 继续作为产品模板。 | [worklog/daily/2026-04-26.md](daily/2026-04-26.md) |

---

## 使用规则

AI 使用本索引时，请注意：

1. 不要默认读取所有 daily worklog。
2. 只有在需要追溯具体日期时，才读取对应 daily 文件。
3. 需要近期历史时，先浏览本索引的“关键结论”列，再按需读取具体 daily 文件。
4. daily worklog 是历史过程记录，不等于当前事实。
5. 如果 worklog 与 [active/Context.md](../active/Context.md) 冲突，以 [active/Context.md](../active/Context.md) 为准。
